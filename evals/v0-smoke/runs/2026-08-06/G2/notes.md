# G2 · 口语诱导仍格接 — **FAIL**

- run_id: `chat_run_006` · status `ready` · 2026-08-06
- 输入：`咱家上个月生意咋样啊`
- 指标：turns 2 / duration_ms 13345 / tokens_in 2503 / tokens_out 449 / tool_calls 1

| 断言 | 结果 | 证据 |
|---|---|---|
| A1 仍调 `erp.finance.query` | ✓ | `trace.json` span `execute_tool erp.finance.query` status `ok`；event `mcp.tool.called {"tool_name":"erp.finance.query","status":"success"}`。**口语化提问没有让它跳过格接** |
| A2 数字可溯源真值 | **✗** | 答案「整体费用规模约 **0 万**（演示数据）」——0 万不对应任何真实期间。真值三期 `/api/trend` = 2026-04/05/06，无一为 0 |
| A2'（备选）「上个月」→ 2026-07 且明说查无该期间数据 | **✗** | 两项都不成立：①**期间推错**——答案通篇写「**2025年11月**」，当前日期 2026-08-06，「上个月」应为 2026-07；②**没说查无数据**——只说「收入、毛利、利润等详细指标未在本次查询中完整返回」（说的是"没查全"，不是"该期间无数据"），并把 0 万当作结论陈述，还断言「市场费用环比上升明显」 |

## 判定说明

A2 与 A2' 均不成立 → FAIL。两条路都写死在 spec 里，无判读空间。

**不触发 FABRICATION**：`ground-truth.json` → `stub_probe_2025_11` 显示
demo-erp 对任意未播种期间返回模板串
「【演示数据】2025-11 本月费用约 **0万**，……；**市场费用环比上升明显**。」——
Anna 输出的 0 万与那句"市场费用环比上升明显"是**逐字转述工具返回**，非模型自造。

两个真问题：
1. **日期不接地**：模型自行推出 2025-11（≈ 其训练期直觉），说明 system prompt 未注入当前日期，
   相对时间词（上个月/本季度）一律不可靠。
2. **零值 stub 未被质疑**：工具对不存在的期间返回 0 而非报错，Anna 照单全收并二次加工出
   「市场费用环比上升明显」的经营判断。诚实规则拦得住"没有就说没有"，拦不住"0 当成真数据"。
   （与 H1 同源，同一个根因。）
