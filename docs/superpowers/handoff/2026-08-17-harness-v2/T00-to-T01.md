# Anna Harness v2 · T00 → T01 Handoff

> Date: 2026-08-17
> Branch: `rewrite/harness-v2`
> T00 base: `b49cc1c6d862115d818beb6f3163aac442e31e52`
> Pre-review checkpoint: `d701a045fcc8bf68b066e87b83396b2ba823ffb5`
> Final T00 commit: the commit containing this handoff; resolve with `git rev-parse rewrite/harness-v2`

## 1. T00 outcome

T00 is complete. The repository now has:

- `packages/harness-v2`: Pi-free TypeScript domain contracts, runtime schemas and public ports;
- `apps/harness-service`: a minimal Node HTTP shell exposing only `GET /health` and `GET /version`;
- npm workspace wiring and focused `harness:v2:test` / `harness:v2:typecheck` commands;
- architecture gates preventing Pi, Electron, Crew, legacy Python and SQLite imports from the new production sources.

T00 did not add Pi, SQLite, Tool execution, Crew behavior, desktop cutover or any T01 implementation.

## 2. Public contracts frozen by T00

- Branded IDs include Workspace, Channel, Channel Session, Run, Event, Stream, Artifact, Memory Candidate, Run Profile, Permission Scope, Command, Worker Profile and Actor IDs.
- Runtime schemas cover ChannelSession, StartRun/Run, CanonicalEvent, Artifact, MemoryCandidate, Budget and terminal RunOutcome.
- `RunOutcome` contains only `completed | awaiting_input | awaiting_approval | failed | timed_out | cancelled`; `running` remains a lifecycle state and is rejected as a terminal outcome.
- Public ports are `LoopKernel`, `EventSink`, `EventStore`, `ToolGateway`, `SandboxAdapter`, `MemoryPolicy`, `EvalGate` and `Scheduler`.
- Scheduled Runs carry trigger and notification audience; StartRun already carries Channel scope, budget, permission scope and stop condition.

## 3. Files added or changed

Root:

- `package.json`
- `package-lock.json`

Contracts package:

- `packages/harness-v2/package.json`
- `packages/harness-v2/tsconfig.json`
- `packages/harness-v2/vitest.config.ts`
- `packages/harness-v2/src/schema.ts`
- `packages/harness-v2/src/contracts.ts`
- `packages/harness-v2/src/interfaces.ts`
- `packages/harness-v2/src/index.ts`
- `packages/harness-v2/test/contracts.test.ts`
- `packages/harness-v2/test/dependency-boundary.ts`

Service shell:

- `apps/harness-service/package.json`
- `apps/harness-service/tsconfig.json`
- `apps/harness-service/vitest.config.ts`
- `apps/harness-service/src/index.ts`
- `apps/harness-service/test/health.test.ts`
- `apps/harness-service/test/architecture.test.ts`

No file was deleted relative to the T00 base.

## 4. TDD evidence

Observed red states before their corresponding implementation:

1. Contracts test could not resolve `./index` before the public package existed.
2. Service health test could not resolve `../src/index`; the version test then received 404 before `/version` existed.
3. The dependency negative fixture initially polluted the production scan, producing 1 failed / 8 passed; moving tests outside `src` made the retained negative gate pass.
4. `npm run harness:v2:test` initially failed with `Missing script: "harness:v2:test"`; workspace wiring made it green.
5. A temporary manifest version `0.0.1` exposed the hard-coded `0.0.0` response; `/version` now reads its package manifest.
6. Review regression tests failed before Budget export/decimal cost, Event timestamp validation, Scheduler notification audience and test-only dependency-gate fixes were implemented.

Final focused result: Harness contracts 11/11 and service shell 4/4.

## 5. Independent review

The review fixed point was `b49cc1c6d862115d818beb6f3163aac442e31e52`. Separate GPT-5.6 Sol xhigh reviewers ran the Standards and Spec axes.

Standards findings resolved:

- replaced URL `.pathname` usage with `fileURLToPath` for Windows portability;
- normalized public validation failures to `SchemaValidationError`;
- included type-contract tests in package `tsc --noEmit`;
- moved the dependency scanner out of the production API into test support;
- removed 23 unrelated npm peer-metadata lockfile changes.

One judgement-call finding was not adopted: the two workspace architecture tests keep small local scanners instead of adding a cross-workspace test-support abstraction.

Spec findings resolved:

- exported `budgetSchema` / `parseBudget` and allowed positive decimal cost limits;
- added Scheduler notification audience to the frozen contract;
- aligned CanonicalEvent with the spec's `timestamp` vocabulary and rejected invalid timestamps.

## 6. Verification

Passed after review fixes:

```text
npm run harness:v2:test
# @anna/harness-v2: 11 passed
# @anna/harness-service: 4 passed

npm run harness:v2:typecheck
npm run typecheck
npm test -- --reporter=dot
# 50 files, 629 passed

./.venv/bin/python -m pytest -q
# 1040 passed, 53 existing deprecation warnings

npm run build
# passed; existing chunk-size warning only
```

`npm ci --ignore-scripts` also passed without changing the final lockfile; its SHA-256 was `cdec9223a5f9a3e90a3e22f4fac56a9d90c8b0188420c8b6a0c175e3196e84be` at that check.

## 7. Safety and known gaps

- Crew and the legacy Python Harness remain frozen; their 1040-test suite stayed green.
- No Pi package, SQLite implementation, secret, runtime database, `.anna`, `.venv`, `node_modules` or `dist` file entered the change.
- No push, merge, rebase or deploy occurred.
- Windows portability defects found in review were fixed, but this Session did not execute the suite on a Windows host.
- npm audit still reports the pre-existing 18 dependency findings; no audit fix was attempted.
- `docs/agents/issue-tracker.md` is still absent. The local Spec/T00 paths supplied the Spec review source.
- The service shell is intentionally not wired into Electron and only binds an ephemeral localhost port in tests. Runtime startup/cutover is outside T00.

## 8. T01 inputs and boundary

T01 is `docs/superpowers/plans/2026-08-17-harness-v2/T01-pi-loop-kernel-canary.md`.

Required inputs now available:

- `LoopKernel` and `EventSink` public seams;
- scoped StartRun, Budget, CanonicalEvent and explicit terminal-outcome schemas;
- focused npm workspace commands;
- a dependency gate that keeps `packages/harness-v2` production source Pi-free.

Implementation note: because `packages/harness-v2` is intentionally Pi-free, T01 should place `PiLoopKernel` in a separate adapter package and make it the only module that imports pinned `pi-agent-core` and `pi-ai` `0.84.2`. Do not weaken the T00 domain boundary to put Pi inside the contracts package.

T01 must start in a fresh Session/context, use TDD, run deterministic fake-provider gates before any live canary, and stop after T01 review. It must not add Crew behavior, SQLite, file/Bash/network Tools, desktop cutover or T02 work.
