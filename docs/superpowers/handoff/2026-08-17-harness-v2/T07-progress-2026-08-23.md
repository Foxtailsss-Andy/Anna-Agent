# T07 Progress Handoff · 2026-08-23

## Fixed Point and Boundary

- Repository: `Anna`
- Branch: `rewrite/harness-v2`
- Fixed point: `5aecdac8fc4f8bac1cfaa54a4f2b57a0fac2c936`
- The worktree was already dirty when this slice started. Existing changes were preserved; no reset, clean, checkout, rebase, push, merge, deploy, or release was performed.
- This handoff covers T07 evidence, v2 capability boundaries, Run lineage, Pi restore, and resumable SSE. T08 desktop cutover and Legacy deletion are explicit remaining Goal gates.

## Completed in This Slice

### Evidence and live boundary

- `evals/harness-v2/t07-fixture-001/` contains `prompts/review.json`, fixture provider/connector responses, `ground-truth.json`, and `manifest.json` with SHA-256 file hashes.
- `scripts/build-evidence-manifest.mjs` and `scripts/verify-evidence-manifest.mjs` provide deterministic manifest generation and fail-closed verification.
- `scripts/live-t07-e2e.mjs` requires an explicit evidence directory, performs source/head/backend/provider/approval preflight, then invokes the opt-in `test/live-t07-production.test.ts` live runner. It never invokes the fixture Vitest test as live evidence; missing adapters or failed live execution return one stable JSON error and still produce a manifest.
- The runner's source-unavailable path returns one JSON error with exit code `2`; the generated `preflight.json` and manifest verify successfully.

### v2 surface boundary

- Without the opt-in bridge, Python `/api/harness/v2/capabilities` and the standalone Node service expose Create/Cowork/Hub as `unsupported` with explicit reason and `409 legacy_surface_not_migrated` on v2 start requests. The managed sidecar now exposes the generic durable Runtime contract for all three surfaces when explicitly enabled; this does not prove domain Legacy cutover.
- WebSearch now has a bounded HTTP provider contract and is advertised as available only when an endpoint is configured. No external provider evidence is claimed here.
- Existing Legacy `/api/create/*` and `/api/cowork/*` routes were not changed during T07. The 409 contract is for the v2 migration boundary; it is not evidence that T08 Legacy cutover is complete.

### Run, Tool, and projection contracts

- Child attribution is carried from `StartRun` through Pi Run events, `ToolRequest`, Tool lifecycle, approval, and Effect events.
- InMemory and SQLite family discovery includes claimed child commands before the child emits its first event.
- Harness-state projection includes queued command-only children and preserves parent/lane attribution.
- A named `DurableEventSink`/`EventReader` contract is exported; Pi restore refuses completed/failed/timed-out/cancelled Runs and carries forward persisted turn count.
- `packages/event-store/src/run-runtime.ts` now binds `RunManager`, a real `EventStore`, and `LoopKernel`: it persists `run.queued`, drives one kernel execution per `(workspace, channel, run)`, refuses implicit restart of a persisted running Run, supports explicit `resume()`, and seals a missing terminal with durable `run.failed`.
- The Runtime bridge has four SQLite-backed tests covering idempotent claim, channel-scoped Run identity, close/reopen resume, kernel failure, and durable terminal precedence.

### SSE and restart recovery

- `from_seq` reconnect tracks the first sequence discontinuity and stops automatic reconnect with an explicit error frame.
- FrameJournal telemetry exposes attach/resume, emitted frame, gap recovery, terminal, and sequence state through `/api/chat/runs/{run_id}/telemetry`.
- Startup stale-run reconciliation writes one durable `error` frame for an interrupted Run and is idempotent.

### Local bridge and canary evidence

- `apps/harness-service/src/index.ts` accepts an optional `HarnessV2Runtime` seam for local integration tests. The default service still reports Create/Cowork/Hub as `unsupported` and returns `409 legacy_surface_not_migrated`; injected test runtimes are reported as `test_only` and injected live runtimes as `available` only for that process.
- `run_command` now preserves process `exitCode/stdout/stderr` as durable command evidence even when the underlying command exits non-zero; invalid input and sandbox failures remain Tool failures.
- The disposable local live canary passed with `ANNA_T07_LIVE_BACKEND_ORIGIN=http://127.0.0.1:8001 npm run test --workspace=@anna/harness-v2 -- --reporter=dot --testNamePattern='live run creates its development worktree only after review approval'`: approval-before-worktree, source cleanliness, real local backend session `200`, actual Vitest execution, and worktree dependency bridge were verified. It uses a temporary repository, InMemory Event Store, and injected approval provider, so it is local-canary evidence, not production T07 evidence.
- The real-provider Pi canary also passed separately with `ANNA_T01_LIVE=1 npm run test --workspace=@anna/pi-loop-kernel -- --reporter=dot --testNamePattern='live-canary'`: `completed`, contiguous sequence `0..32`, one terminal event, and usage present. It is provider/Kernel evidence only and does not prove T07 Review-to-Validated-Patch production wiring.

## Verification

- `npm run typecheck` passed across root, Harness v2, Event Store, Scheduler, Pi Loop Kernel, Harness service, Trace, and Eval.
- `npm test -- --reporter=dot` passed: root `53 files / 634 tests`; Harness v2 `98 passed / 2 skipped`; Event Store `99`; Scheduler `20`; Pi Loop Kernel `23 passed / 1 skipped`; Harness service `6`; Trace `12`; Eval `10`.
- `./.venv/bin/pytest -q` passed: `1045 passed, 53 warnings`.
- `npm run build` passed.
- `npm run evidence:verify -- evals/harness-v2/t07-fixture-001/manifest.json` passed.
- T07 live runner subprocess check passed as a truthful blocker: exit `2`, one JSON error, generated evidence manifest verified.
- `git diff --check` passed.

## Remaining Production Blockers

1. The Create vertical slice now has an explicit Python API -> TS sidecar -> Pi -> SQLite Event Store bridge. It is opt-in through `ANNA_HARNESS_V2_BRIDGE_ENABLED=1`; default Desktop/API behavior remains unsupported/409. The managed sidecar also exposes the same generic contract for Cowork/Hub, but their domain behavior and T08 Legacy deletion/Desktop cutover are still incomplete.
2. Review approval, owner identity, provider ownership, and T07 Review-to-Validated-Patch production canary are still not connected. The live Runtime canary below proves a real provider/Tool/Skill/Eval Run, not Review approval or patch validation.
3. WebSearch has a bounded provider contract, but no configured external connector and no live WebSearch evidence are present in this worktree.
4. Pi restore now persists and validates a SHA-256 provider/model, RunProfile, and system-prompt fingerprint. Durable sinks append `run.usage.updated` cumulative token evidence and preserve cost only when the provider reports a non-zero cost. A real external-provider Pi transcript close/reopen run remains pending.
5. FrameJournal now retries failed writes in order and records a durable `run.durability_gap` marker; cold restart converts the marker into persisted `failed/durable_gap` Run state and an explicit recovery error frame. A real production storage outage drill remains unverified.

## 2026-08-23 Runtime Bridge Continuation

- `apps/harness-service/src/runtime.ts` now reads Run events directly from the requested Channel-scoped SQLite store after validating the durable command, so event reads survive service restart and never rely on an in-process scope map.
- `apps/harness-service/src/production.ts` and `src/main.ts` provide a real OpenAI-compatible Pi sidecar with SQLite Event Store, immutable Skill/profile snapshot, bounded `read_only` ToolGateway, usage preservation, and required contract Eval persisted as `run.eval.contract` before the terminal event. Eval failure downgrades the terminal outcome to `run.failed`.
- `services/api/app/harness_v2_bridge.py` and `services/api/app/routes/harness_v2.py` provide an explicit Python API bridge for capabilities, Create start, and Run event reads. Without `ANNA_HARNESS_V2_BRIDGE_ORIGIN`, the existing fail-closed 409 contract remains unchanged.
- `apps/desktop/electron/runtime-service.mjs` starts the bundled sidecar only with explicit `ANNA_HARNESS_V2_BRIDGE_ENABLED=1`, allocates a separate port, health-checks it, passes a separate Harness v2 SQLite path, and stops it with the Python runtime. The sidecar is included in the desktop build path through `harness:v2:build`.
- `apps/harness-service/test/live-runtime.test.ts` passed with `ANNA_HARNESS_V2_LIVE=1`: the persisted redacted summary is in `evals/harness-v2/live-runtime-2026-08-23/summary.json`, and its hash manifest is `evals/harness-v2/live-runtime-2026-08-23/manifest.json`. It records 79 contiguous events, one `run.completed`, one completed Tool call, provider usage, and a passed contract Eval. No prompt, model response, or secret is included.
- A separate real-process Python API bridge check passed with `create=available`, HTTP `202 queued`, contiguous events, one terminal `run.completed`, `run.eval.contract.passed=true`, and provider usage present.

## Verification After Continuation

- `npm run typecheck`: passed.
- `npm test -- --reporter=dot`: passed; root 634, Harness v2 98 passed/2 skipped, Event Store 99, Scheduler 20, Pi Loop Kernel 23 passed/1 skipped, Harness service 8 passed/1 skipped, Trace 12, Eval 10.
- `./.venv/bin/pytest -q`: passed.
- `npm run build`: passed.
- `npm run harness:v2:build`: passed; sidecar emitted `apps/harness-service/dist/main.js`.
- Fixture and live evidence manifests both passed `verify-evidence-manifest.mjs`.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, 17 tests.
- `git diff --check`: passed.

## 2026-08-23 Durable Resume and Live Owner Adapter Continuation

- `packages/event-store/src/lifecycle.ts` now distinguishes suspend events (`run.awaiting_input` and `run.awaiting_approval`) from final events. Suspended Runs retain their status but can append `run.resumed` after an explicit decision; completed/failed/timed-out/cancelled streams remain sealed.
- `packages/event-store/src/run-manager.ts` and `src/run-runtime.ts` preserve suspended state, require explicit `resume()` to drive it, and do not reconcile an approval-waiting Run as a crashed Run.
- `packages/pi-loop-kernel/src/pi-loop-kernel.ts` validates the persisted execution fingerprint before calling the provider, restores cumulative usage from durable events, and emits `run.usage.updated` only when the sink is durable.
- `apps/harness-service/test/runtime.test.ts` proves Pi transcript continuation through a real SQLite file close/reopen with a test provider; the test is deliberately not reported as external-provider evidence.
- `packages/harness-v2/src/live-review-approval.ts` implements the real Owner boundary. It POSTs to `<ANNA_T07_LIVE_APPROVAL_ORIGIN>/decisions` with `ownerId` and one action: `confirm_memory_candidate`, `approve_lane`, or `approve_effect`. The response must be `{ "approved": boolean, "actorId": "<same owner>" }`.
- `packages/harness-v2/test/live-t07-production.test.ts` uses the real provider Run API first, then real SQLite Event Store, Channel Memory, Trace, Eval, Scheduler, and the HTTP Owner adapter for the T07 live scenario. It writes only a redacted `summary.json` to `ANNA_T07_LIVE_EVIDENCE_DIR`; it is skipped without all explicit live inputs.

## Verification of Durable Resume Slice

- `npm run test --workspace=@anna/event-store -- --run`: passed, 100 tests.
- `npm run test --workspace=@anna/pi-loop-kernel -- --run`: passed, 25 passed / 1 skipped.
- `npm run test --workspace=@anna/harness-service -- --run`: passed, 9 passed / 1 skipped.
- `npm run test --workspace=@anna/harness-v2 -- --run test/live-review-approval.test.ts test/live-t07-production.test.ts`: passed, 2 passed / 1 skipped.
- `npm run typecheck --workspace=@anna/harness-v2`: passed.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, 17 tests.
- `node --check scripts/live-t07-e2e.mjs`: passed.
- `npm run live:t07` without operator inputs: exit `2`, one stable `t07_live_operator_input_required` JSON error.

## Remaining Production Blockers After This Slice

1. The new live runner is implemented but not executed successfully in this environment: a real Owner service must expose the documented `/status` and `/decisions` contract, and a real clean source checkout must be supplied. Until that run produces evidence, T07 production canary remains unverified.
2. The live provider Pi transcript close/reopen path has SQLite/fake-provider proof but still needs an opt-in external-provider run with process restart and redacted evidence.
3. Create/Cowork/Hub generic Runtime routes are opt-in behind `ANNA_HARNESS_V2_BRIDGE_ENABLED=1`; domain-specific Legacy migration, T08 Legacy deletion, and Desktop cutover are not complete.
4. WebSearch has a configured-endpoint provider contract, but no live external provider evidence is present.
5. FrameJournal durable-gap recovery is implemented and tested; a production storage outage drill remains unverified.

## Final Verification After This Continuation

- `npm run typecheck`: passed.
- `npm test -- --reporter=dot`: passed; root 53 files / 634 tests, Harness v2 100 tests with 3 live skips, Event Store 100 tests, Scheduler 20, Pi Loop 25 with 1 existing live skip, Harness service 9 with 1 live skip, Trace 12, Eval 10.
- `./.venv/bin/python -m pytest -q`: passed, `1046 passed, 53 warnings`.
- `npm run build`: passed with the existing chunk-size warning.
- `npm run harness:v2:build`: passed.
- `npm audit --json`: passed with `0 low / 0 moderate / 0 high / 0 critical`. The release dependency floor is now Vite 7.3.6, Electron 43.4.1, Electron Builder 26.15.3, with secure overrides for esbuild, nanoid and postcss.
- `npm run desktop:package`: produced `release/mac-arm64/Anna.app` and `app.asar`. Code signing was skipped because this machine has no valid Developer ID identity; signed release acceptance remains unverified.
- `npm run desktop:smoke-asar`: passed; packaged API health returned `ok`, with model/MCP status honestly reported as `not_configured`.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, 17 tests. The broader `tests/frontend/app_shell_smoke.mjs` command still has 13 failures because it imports paths under `apps/desktop/src/features/` that do not exist in this worktree; this is recorded as a desktop test/structure blocker, not converted into a pass.
- Fixture and existing live-runtime evidence manifests both verify successfully. `npm run live:t07` without operator inputs exits `2` with `t07_live_operator_input_required`; no real T07 canary was claimed.

## Next Ticket Inputs

- Execute the new live T07 runner with a real Owner `/decisions` provider and preserve its manifest as production evidence.
- Add an external-provider Pi close/reopen canary using the persisted fingerprint and cumulative usage ledger.
- Add a persistence-failure integration test and make the server terminate/recover with an explicit durable gap outcome.
- Keep fixture, fake-provider, local-canary, and production evidence in separate directories and reports.

## 2026-08-23 FrameJournal Durable Gap Continuation

- `services/runtime/app/frame_journal.py` now keeps failed write-through frames in sequence order, retries them on later appends and driver close, and exposes `persistence_failure_count`, `durable_seq`, `pending_persistence_seqs`, and `durability_degraded` telemetry.
- `services/api/app/routes/chat.py` records a durable `run.durability_gap` audit marker when a terminal background run still has pending frames. After a cold restart, a run with that marker and no durable terminal frame emits and best-effort persists an explicit `durable_gap` error outcome rather than returning an empty stream.
- `tests/runtime/test_frame_journal.py` covers ordered retry and permanent terminal persistence failure. `tests/chat/test_background_runs.py` covers a real manager run, SQLite frame-write outage, cold restart, recovery emission, and recovery persistence.

## Verification After FrameJournal Continuation

- `./.venv/bin/python -m pytest -q tests/runtime/test_frame_journal.py tests/chat/test_background_runs.py tests/gates/test_gate_p3_disconnect.py tests/gates/test_gate_continue.py`: passed, 29 tests.
- The full Python suite and TypeScript/build/package gates must be rerun after the remaining Goal slices are complete.

## Remaining Production Blockers After FrameJournal Continuation

1. The new live T07 runner still requires a real Owner `/status` and `/decisions` service, an owner identity, a real provider, and a clean source checkout. No production Review-to-Validated-Patch canary is claimed without those inputs.
2. External-provider Pi transcript close/reopen evidence with a process restart remains pending; local SQLite/fake-provider proof is separate.
3. Create remains opt-in behind `ANNA_HARNESS_V2_BRIDGE_ENABLED=1`; Cowork/Hub migration, T08 Legacy deletion, and Desktop cutover remain incomplete.
4. WebSearch provider integration remains incomplete.
5. Signed Desktop release acceptance remains unverified because this machine has no valid Developer ID identity; the unsigned package smoke is separate evidence.

## 2026-08-23 Surface and WebSearch Continuation

- `apps/harness-service/src/production.ts` now enables the same durable Runtime/Event Store contract for `create`, `cowork`, and `hub` by default. The generic contract is available only through the opt-in bridge; domain-specific Legacy behavior and Desktop cutover remain separate migration work.
- `apps/harness-service/test/health.test.ts` proves all three surface routes return `202` through an injected Runtime and that the sidecar reports each surface consistently.
- `packages/pi-loop-kernel/src/pi-loop-kernel.ts` now exposes `web_search` only when the resolved RunProfile allows it. The request still passes through Anna ToolGateway and produces normal Tool lifecycle events.
- `apps/harness-service/src/production.ts` provides a bounded HTTP WebSearch provider contract: `POST {query,max_results}`, optional Bearer key, normalized `{title,url,snippet}` results, and fail-closed provider errors. The sidecar advertises WebSearch as available only when an endpoint is configured; otherwise it remains unsupported.
- `packages/pi-loop-kernel/test/live-restore-canary.test.ts` is an explicit external-provider close/reopen runner. With `ANNA_T01_LIVE_RESTORE=1`, it persists a transcript, simulates process interruption after the transcript commit, closes SQLite, reopens it, validates the same execution fingerprint, and records redacted restart evidence. It is skipped by default and was not claimed as live evidence in this environment.
- The live T07 preflight now requires the sidecar to verify Owner `GET /status` before reporting `review_gate.status=ready`; the runner executes the test from the clean `ANNA_T07_LIVE_SOURCE` checkout that it validated, not from the caller's dirty checkout.

## Verification After Surface and WebSearch Continuation

- `npm run typecheck`: passed across root and all Harness v2 workspaces.
- Harness service tests: `15 passed / 1 skipped`, including the surface contract, Owner readiness, and WebSearch provider tests.
- Pi Loop Kernel tests: `26 passed / 2 skipped`; the existing provider canary and the new external-provider restore canary remain skipped without explicit operator input.

## Operator Execution Commands

The following commands require external inputs and were intentionally not run with fabricated values.

### Review-to-Validated-Patch live canary

The Owner service must implement:

- `GET /status` -> `{"status":"ready","owner_id":"<owner>"}`
- `POST /decisions` -> `{"approved":true|false,"actorId":"<same owner>"}`

The POST request includes `ownerId` and one of `confirm_memory_candidate`, `approve_lane`, or `approve_effect`; the service must authenticate the owner and evaluate the supplied trace/artifact identifiers.

Run from a clean checkout with dependencies installed and a configured real provider:

```sh
export ANNA_T07_LIVE_SOURCE=/absolute/path/to/clean/anna-checkout
export ANNA_T07_LIVE_HEAD=$(git -C "$ANNA_T07_LIVE_SOURCE" rev-parse HEAD)
export ANNA_T07_LIVE_BACKEND_ORIGIN=http://127.0.0.1:8001
export ANNA_T07_LIVE_OWNER_ID=owner-actual
export ANNA_T07_LIVE_PROVIDER=provider-actual
export ANNA_T07_LIVE_APPROVAL_ORIGIN=http://127.0.0.1:9010
export ANNA_T07_LIVE_EVIDENCE_DIR=evals/harness-v2/live-t07-production-2026-08-23
npm run live:t07
npm run evidence:verify -- "$ANNA_T07_LIVE_EVIDENCE_DIR/manifest.json"
```

The backend must run with `ANNA_HARNESS_V2_BRIDGE_ENABLED=1`, the sidecar must use the same `ANNA_T07_LIVE_APPROVAL_ORIGIN` and `ANNA_T07_LIVE_OWNER_ID`, and `/api/admin/runtime/status` must report the configured real model. Exit code `2` remains a truthful blocker and must not be converted to evidence of success.

### External Pi transcript restore

```sh
export ANNA_T01_LIVE_RESTORE=1
export ANNA_T01_LIVE_RESTORE_EVIDENCE_DIR=evals/harness-v2/live-pi-restore-2026-08-23
npm run test --workspace=@anna/pi-loop-kernel -- --run test/live-restore-canary.test.ts
npm run evidence:manifest -- "$ANNA_T01_LIVE_RESTORE_EVIDENCE_DIR"
npm run evidence:verify -- "$ANNA_T01_LIVE_RESTORE_EVIDENCE_DIR/manifest.json"
```

The source checkout must contain `.anna/runtime.json` with a real OpenAI-compatible provider. The canary itself performs the provider call, closes SQLite, reopens it, validates the same fingerprint, requires `run.resumed`, one terminal event, and contiguous sequence evidence.

### Configured WebSearch

Add these fields to the runtime config used by the Harness v2 sidecar, without committing the secret:

```json
{
  "web_search_endpoint": "https://your-search-adapter.example/v1/search",
  "web_search_api_key": "<local-secret>"
}
```

The adapter receives `POST {"query":"...","max_results":5}` and must return `{ "results": [{ "title":"...", "url":"...", "snippet":"..." }] }`. The sidecar advertises WebSearch only after this endpoint is configured; a real WebSearch run still needs its own redacted live evidence.

## Final Verification After Owner Preflight Fix

- `npm run typecheck`: passed.
- `npm test -- --reporter=dot`: passed; root `53 files / 634 tests`, Harness v2 `100 passed / 3 skipped`, Event Store `100`, Scheduler `20`, Pi Loop Kernel `26 passed / 2 skipped`, Harness service `15 passed / 1 skipped`, Trace `12`, Eval `10`.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `npm run build` and `npm run harness:v2:build`: passed; sidecar emitted `apps/harness-service/dist/main.js`.
- `npm audit --json`: passed with `0` vulnerabilities across all severities.
- `npm run desktop:package`: passed and produced `release/mac-arm64/Anna.app`; code signing skipped because this machine has no valid Developer ID identity.
- `npm run desktop:smoke-asar`: passed; packaged API health `ok`, model/MCP `not_configured`.
- `npm run evidence:verify` passed for the fixture and existing live-runtime manifests.
- `node --test tests/frontend/electron_runtime.test.mjs tests/frontend/runtime_info_path.test.mjs`: passed, `19` tests.
- `node scripts/live-t07-e2e.mjs` without the seven operator inputs: exit `2`, one `t07_live_operator_input_required` JSON error; no production T07 evidence claimed.

## Remaining Goal Gates

1. Execute `npm run live:t07` with the seven required `ANNA_T07_LIVE_*` inputs, a real Owner `/status` + `/decisions` service, owner identity, provider configuration, and clean source checkout; only its redacted manifest can close the production Review-to-Validated-Patch gate.
2. Execute `ANNA_T01_LIVE_RESTORE=1` with the external provider and preserve `ANNA_T01_LIVE_RESTORE_EVIDENCE_DIR` plus a generated evidence manifest.
3. Wire the Desktop UI and each domain's real Legacy behavior to the new surface contract, then remove the 409 compatibility boundary only after production evidence. The generic sidecar route alone does not prove this cutover.
4. Provide signed Desktop acceptance on a machine with a Developer ID identity; current package evidence is unsigned.

## 2026-08-23 Classic Eval Fixture Evidence

- `packages/eval/test/export-fixture-evidence.test.ts` now provides an explicit opt-in exporter for the fixed Smoke 4 and Dev 16 sets. The default test run skips export; setting `ANNA_EVAL_EVIDENCE_DIR` writes raw case JSON, tool trajectories, terminal events, release-gate results, first Trace divergence, regression/badcase output, and ground truth.
- Generated fixture evidence is isolated at `evals/harness-v2/eval-fixture-2026-08-23/` and contains 23 hashed files: 20 raw cases under `cases/`, `regression-dev-16.json`, `ground-truth.json`, `report.json`, and `manifest.json`.
- The fixed fixture result is Smoke `4/4` passed; Dev `15/16` passed and `1` blocked. `dev-16` is classified as `contract_failure`; its first divergence is event `dev-16:prohibited-effect`, type `tool.effect.succeeded`, reason `prohibited_effect:prohibited`.
- Fixture metrics are explicitly `latencyMs: null`, `cost: null`, and `metricsSource: fixture_only_not_production`; this evidence does not close the real-provider evaluation gate.
- Export and verification commands:

```sh
ANNA_EVAL_EVIDENCE_DIR=/absolute/path/to/anna/evals/harness-v2/eval-fixture-2026-08-23 \
  npm run test --workspace=@anna/eval -- --run
ANNA_EVIDENCE_CASE_ID=harness-v2-eval-fixture \
ANNA_EVIDENCE_MODE=fixture \
ANNA_EVIDENCE_PROVIDER=fixture \
  npm run evidence:manifest -- /absolute/path/to/anna/evals/harness-v2/eval-fixture-2026-08-23
npm run evidence:verify -- /absolute/path/to/anna/evals/harness-v2/eval-fixture-2026-08-23/manifest.json
```

- Fixed point recorded in the manifest: `5aecdac8fc4f8bac1cfaa54a4f2b57a0fac2c936`.
- SHA-256: `manifest.json` `37a7011a11a557a319250cdae62f77f8da2260790bb4c7eb4b8b3d591bacab24`; `ground-truth.json` `f537c02eef1898602f83b970ee77a5e380a57449e4e9c0d1f5d0721d0b7f2477`; `report.json` `3e1581391eb19b3fce5748c7c3be7a67386ed5eb32b189dbd2d003b98a785101`; `regression-dev-16.json` `117f2c52e1eebd9ea6e93ef8cbfbe230042d87607ad848a2a593d535a5998894`.

## Verification After Classic Eval Evidence

- `npm run typecheck`: passed.
- `npm test -- --reporter=dot`: passed; root `53 files / 634 tests`, Harness v2 `100 passed / 3 skipped`, Event Store `100`, Scheduler `20`, Pi Loop Kernel `26 passed / 2 skipped`, Harness service `15 passed / 1 skipped`, Trace `12`, Eval `10 passed / 1 skipped`.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `npm run evidence:verify -- evals/harness-v2/eval-fixture-2026-08-23/manifest.json`: passed, `23` files.
- `git diff --check`: passed.

## 2026-08-23 External Pi Restore and Sink-Failure Continuation

- `packages/pi-loop-kernel/src/pi-loop-kernel.ts` now aborts the active Pi Agent when a durable `EventSink.append` fails and ignores subsequent Agent events after `sinkFailure` is recorded. This prevents a provider/lifecycle failure message from being appended after the original durable write failure.
- `packages/pi-loop-kernel/test/pi-loop-kernel.test.ts` adds the regression assertion that a sink failure does not persist a later Pi transcript or `run.failed` event.
- `packages/pi-loop-kernel/test/live-canary.test.ts` uses a non-empty but non-exposed `read_workspace` Tool policy so the single-turn provider canary does not drift into an uncontrolled Tool loop. The real provider canary passed with `completed`, one terminal, contiguous `0..87` sequence and usage present.
- `packages/pi-loop-kernel/test/live-restore-canary.test.ts` now interrupts after a persisted `toolResult`, aborts the first Agent process, reopens SQLite with an approved ToolGateway, and records only redacted event types/roles. This avoids claiming restore from an assistant-final transcript that Pi `Agent.continue()` explicitly rejects.
- External provider restore passed:
  - outcome `completed`;
  - `run.resumed=true`;
  - one terminal event;
  - contiguous sequence `0..230`;
  - usage evidence present.
- Live evidence is isolated at `evals/harness-v2/live-pi-restore-2026-08-23/`. Its manifest has `evidenceMode=live`, `provider=external-openai-compatible`, one redacted summary file, and verifies successfully. Summary SHA-256: `ee0ea75ced28242cf5347e8df6f5c45093fdcf22862710e27b68710ce712a93a`; manifest SHA-256: `66ca5f1e69f9e2c0e15f64a03812b93418486c199b4afb619ed9dd67e57ee65b`.
- The first live restore attempt exposed and then fixed two defects: the canary's empty Tool policy violated RunProfile validation, and durable sink failure allowed trailing Pi events. Failed attempts remain unclaimed as evidence; only the final verified manifest is live evidence.

## Current Remaining Goal Gates

1. T07 Review-to-Validated-Patch production canary still requires a real Owner `/status` + `/decisions` service, owner identity, backend bridge, real provider configuration, and clean source checkout. No Owner or Review Gate result is claimed from fixture/injected runtime tests.
2. Create/Cowork/Hub still need Desktop caller cutover and domain Legacy migration before the 409 compatibility boundary can be removed. The generic sidecar Runtime contract is not sufficient evidence for T08 completion.
3. WebSearch has a bounded provider contract but no configured external WebSearch run/evidence.
4. Signed Desktop acceptance still requires a machine with a valid Developer ID identity.

## Final Local Verification After External Pi Restore

- `npm run typecheck`: passed across root and all packages.
- `npm test -- --reporter=dot`: passed; root `53 files / 634 tests`, Harness v2 `100 passed / 3 skipped`, Event Store `100`, Scheduler `20`, Pi Loop Kernel `26 passed / 2 skipped`, Harness service `15 passed / 1 skipped`, Trace `12`, Eval `10 passed / 1 skipped`.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `npm run build` and `npm run harness:v2:build`: passed. Existing frontend chunk-size warning remains.
- `npm run desktop:package`: passed and produced `release/mac-arm64/Anna.app`; packaging skipped code signing because the machine has zero valid Developer ID identities.
- `npm run desktop:smoke-asar`: passed with packaged API health `ok`, served index `true`, and explicit `modelStatus`/`mcpStatus` `not_configured`.
- `git diff --check`: passed.
- Evidence manifests reverified: T07 fixture `4` files, classic eval fixture `23` files, live runtime `1` file, live Pi restore `1` file.

## 2026-08-23 Real Chat Run Evidence and Audit Summary

- `scripts/live-chat-e2e.mjs` now includes only redacted audit facts in its successful summary: sorted event types, model-reported Tool call count, and whether `skill.loaded.payload.skill_id` is present. It never includes the operator message or assistant content.
- The RED assertion was added to `tests/frontend/live_chat_e2e.test.mjs`; the complete file passed `9/9` after the runner change.
- A temporary local Python backend used the existing configured external OpenAI-compatible provider and an isolated `/tmp` state database. The live runner completed with exit code `0` and reported `status=ready`, `template_id=summarize`, `saved_memory_id=null`, five required audit event types, `audit_tool_call_count=0`, and `audit_skill_id_present=true`.
- Evidence is isolated at `evals/harness-v2/live-chat-2026-08-23/`; its manifest has `evidenceMode=live`, provider `external-openai-compatible`, one redacted summary file, and verifies successfully. Summary SHA-256: `b152aa355d5dfb1709fd152964ebee615c65660607d5132ed2acf68290fd1da8`; manifest SHA-256: `17e0f6baee6338e4273feb357de8d2d156188dfd9392aa049a854f2e60ef11c4`.
- This is real local Chat/Run evidence, not T07 Review-to-Validated-Patch completion: it does not prove the Owner approval bridge, isolated live patch, screenshot, or production Desktop caller cutover.

## Final Verification and Review for Real Chat Evidence

- `node --test tests/frontend/live_chat_e2e.test.mjs`: passed, `9/9`; the new assertion is RED-first and verifies the redacted audit summary contract.
- `npm test -- --reporter=dot`: passed; root `53 files / 634 tests`, Harness v2 `100 passed / 3 skipped`, Event Store `100`, Scheduler `20`, Pi Loop Kernel `26 passed / 2 skipped`, Harness service `15 passed / 1 skipped`, Trace `12`, Eval `10 passed / 1 skipped`.
- `./.venv/bin/python -m pytest -q`: passed on the serial rerun, `1048 passed, 53 warnings`. A concurrent first attempt had one known Crew terminal-signal timing failure; its focused test passed immediately and the serial full rerun passed. No Crew code was changed for this Goal.
- `npm run typecheck`, `npm run harness:v2:build`, `npm run build`, `npm run desktop:smoke-asar`, and `git diff --check`: passed. The existing frontend chunk-size warning remains. The packaged smoke reported API health `ok`, served index `true`, and model/MCP `not_configured`.
- Evidence manifests reverified successfully: T07 fixture `4` files, classic eval fixture `23` files, live runtime `1` file, live Pi restore `1` file, and live Chat `1` file.
- Manual Standards review: no P0/P1 found in the current Chat evidence slice. The summary is redacted, uses existing runner/test patterns, adds no abstraction, and keeps the real-run step opt-in. Manual Spec review: the slice proves a real local Chat/Run with provider, skill, and audit facts, while explicitly leaving Owner approval, patch/screenshot, WebSearch, and Desktop caller cutover unclaimed.
- Formal two-agent review was unavailable in this checkout because `docs/agents/issue-tracker.md` is absent and no multi-agent review tool is configured; the manual two-axis result is recorded here.
- No commit, push, merge, deploy, or release was performed. Existing dirty and untracked worktree changes were preserved.

## 2026-08-23 Desktop v2 Surface Client Seam

- Added `apps/desktop/src/lib/api/harnessV2.ts` as the narrow Desktop client seam for canonical v2 Run start, capability read, and durable event read.
- `startHarnessV2Run()` sends `workspace_id`, `channel_id`, `command_id`, `source_event_id`, `goal`, and optional `run_id` / `parent_run_id` / `parent_event_id` / `lane_id` to exactly one v2 surface endpoint. It does not retry a 409 through a Legacy endpoint.
- `readHarnessV2RunEvents()` reads the same Run with `workspace_id`, `channel_id`, and explicit `from_seq`; the caller owns attach/reconnect policy and the endpoint remains cursor-based.
- Added `apps/desktop/src/lib/api/harnessV2.test.ts`; RED was the missing module, GREEN is `3/3` focused tests. Tests cover canonical attribution, cursor reads, and no Legacy fallback after v2 409.
- Verification: `npm exec --no -- vitest run` passed `54 files / 637 tests`; `npm run typecheck` passed root and all Harness v2 packages; `git diff --check` passed.
- Standards review: no P0/P1 in this slice. Spec review: the client seam is ready for a caller migration but does not claim Create/Cowork/Hub cutover; current UI still needs an approved channel/source mapping and real v2 runtime evidence before switching the production caller.
- Ownership: current Goal session, Desktop API seam and test only; no other dirty files were altered for this ticket. No commit, push, merge, deploy, or release.

## 2026-08-23 Managed Desktop Bridge Live Boundary

- Started Electron's managed Node Harness v2 sidecar and Python API with the existing external OpenAI-compatible runtime config; all state and workspace files were redirected to disposable temporary directories.
- Boundary-only evidence: `evals/harness-v2/desktop-managed-bridge-2026-08-23/` verifies one redacted summary. API and sidecar health were `ok`; capabilities reported `create/cowork/hub=available`, Review Gate `blocked`, and WebSearch `unsupported`. This is a runtime boundary check, not a production gate.
- Real managed-bridge Run evidence: `evals/harness-v2/live-desktop-bridge-2026-08-23/` verifies one redacted summary. Through Python API → managed Node sidecar → external provider, the Run returned HTTP `202`, completed exactly once, emitted `76` contiguous events, reported usage, executed `1` bounded read Tool, and passed Contract Eval.
- Cursor attach was verified through the same bridge with `from_seq=0`: the response contained `75` events starting at seq `1`. No event content, query, provider credential, or local path was stored in the summary.
- Live Run summary SHA-256: `c262065d389c8a70d85e5ce31d4d398986e6a33774b482be0ed54354e8a3436a`; manifest SHA-256: `c0aa72425df1702d2c05633be33be8f91c2634b7d7d38bcd2d3f29105f6aaadc`.
- This closes the real managed bridge/provider Run boundary for local developer preview, but does not close T07 Review-to-Validated-Patch: Owner `/status` + `/decisions`, human approval, isolated patch/screenshot/test scenario, and production Desktop caller cutover remain separate gates.

## Final Verification After Managed Bridge Run

- `npm test -- --reporter=dot`: passed; frontend `54 files / 637 tests`, Harness v2 `100 passed / 3 skipped`, Event Store `100`, Scheduler `20`, Pi Loop Kernel `26 passed / 2 skipped`, Harness service `15 passed / 1 skipped`, Trace `12`, Eval `10 passed / 1 skipped`.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `npm run typecheck`: passed across root and all Harness v2 packages.
- `npm run desktop:package`: passed; current source rebuilt into `release/mac-arm64/Anna.app`. Code signing skipped because the machine has zero valid Developer ID identities.
- `npm run desktop:smoke-asar`: passed with served index `true`, API health `ok`, and packaged Python runtime present; model/MCP are explicitly `not_configured` in the no-secret smoke environment.
- `git diff --check`: passed. All seven evidence manifests reverified: T07 fixture `4`, classic eval `23`, live runtime `1`, live Pi restore `1`, live Chat `1`, managed bridge boundary `1`, and managed bridge live Run `1` files.
- Managed bridge boundary hashes: summary `7289eef60bd243b0d3bc2a4d8f0a0e45c9e79d8f9f7f51911ac7c29ae819d702`; manifest `45d7fe7018517df0c82a2de588280a2b23cedfd631d3f938a3226820a9bf9dfa`.
- No commit, push, merge, deploy, or release was performed; the existing dirty worktree remains protected.

## 2026-08-23 T08 Review Channel Inspector Slice

- Extended `apps/harness-service/src/index.ts` with a bounded read-only contract:
  `GET /v2/channels/:workspaceId/:channelId/events?streamId=&afterSeq=` and
  `GET /v2/runs/:runId/trace?workspaceId=&channelId=&surface=`.
- Channel reads use the URL scope and durable `afterSeq` cursor. Trace uses the existing
  `@anna/trace` live cursor over the same scoped EventStore. Unknown storage, malformed
  cursors, cross-scope reads, and scope widening return explicit errors.
- The service now rejects non-loopback binding. `instanceId` health/version metadata and
  optional `X-Anna-Workspace-ID` / `X-Anna-Channel-ID` checks are available for the managed
  Desktop boundary without changing the existing `/capabilities`, surface start, or Run
  event endpoints.
- Added `apps/harness-service/test/review-api.test.ts`: RED was 6/6 failures, GREEN is
  6/6. The full Harness service suite is `21 passed / 1 skipped`.
- Added `apps/desktop/src/pages/review/reviewApi.ts` and the read-only
  `ReviewChannelInspector` page. The page is an independent `review` shell section and
  calls only `v2ApiBase`; when v2 is not configured it does not fall back to Legacy.
- Electron injects the managed sidecar origin as `v2ApiBase`; restart reuses the existing
  sidecar port. Desktop API tests are `3/3`, including explicit v2 origin, scope headers,
  URL encoding, and no Legacy fallback.

## Verification After T08 Review Channel Slice

- `npm exec --no -- vitest run`: passed, `55 files / 639 tests`.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event Store `100`,
  Scheduler `20`, Pi Loop `26 passed / 2 skipped`, Harness service `21 passed / 1 skipped`,
  Trace `12`, Eval `10 passed / 1 skipped`.
- `npm run typecheck`: passed for root and all Harness v2 workspaces.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, `17/17`.
- `npm run desktop:package` and `npm run desktop:smoke-asar` passed after the final v2
  no-fallback/restart-port patch. Code signing remains skipped because no Developer ID
  identity is installed.
- All seven evidence manifests were reverified after the final source state: T07 fixture `4`,
  classic eval `23`, live runtime `1`, live Pi restore `1`, live Chat `1`, managed bridge
  boundary `1`, and managed bridge live Run `1` files.
- `git diff --check` passed. No commit, push, merge, deploy, or release was performed.

## Remaining Goal Gates After T08 Review Channel Slice

1. T07 still requires the real Owner `/status` + `/decisions` bridge, human approval,
   isolated patch/screenshot/test evidence, and production gate acceptance.
2. Create/Cowork/Hub caller cutover and domain Legacy migration remain pending; the new
   inspector and generic sidecar contract do not remove the existing 409 boundary.
3. WebSearch has a bounded provider contract but no external live run/evidence.
4. Harness-state projection and child Run attribution remain post-MVP until an approved
   caller contract is frozen; current service Trace projection is scoped to existing Run
   streams and does not claim broad surface migration.
5. Signed Desktop acceptance remains unverified without a valid Developer ID identity.

## Final Verification After Owner/Create Completion

- Latest `npm run harness:v2:build` completed successfully after the Owner bridge and
  Create activation changes; `git diff --check` passed.
- Latest `npm run desktop:package` completed for macOS arm64. Code signing was skipped
  because this machine has zero valid Developer ID identities.
- Latest `npm run desktop:smoke-asar` passed with `servedIndex=true`, API health `ok`,
  packaged Python runtime resolved, and model/MCP status `not_configured`.
- No commit, push, merge, deploy, or release was performed; the dirty worktree remains
  protected.

## 2026-08-23 Create Activation Vertical Slice

- `apps/harness-service/src/create-activation.ts` now validates the staged Skill
  artifact path, resolves it inside the configured workspace, bounds the source file,
  verifies its sha256, and copies it once into `skills/<skill_id>/SKILL.md` with an
  exclusive write.
- The v2 activation route remains `409 create_activation_not_implemented` unless
  `workspaceRoot`, Owner approval origin, and owner identity are explicitly configured.
  When configured, it requires `x-anna-owner-id`, asks the durable Owner bridge to
  approve `create.activate:<runId>:<artifactHash>`, and fails closed on denial or
  unavailable approval.
- Activation is recorded on the separate durable stream
  `create-activation:<runId>` because the Event Store correctly fences the completed
  Run stream. The Create projection merges that effect stream and reports `saved` only
  after the activation event is persisted. A persistence failure removes the copied
  target before returning failure.
- The live sidecar now passes its durable Event Store into the HTTP service and exposes
  the configured Create activation adapter; the standalone Owner bridge remains usable
  without model configuration.

## Verification After Create Activation Slice

- `apps/harness-service/test/create-projection.test.ts`: `9 passed`, including real HTTP
  Owner request/decision, bounded file activation, activation stream persistence, and
  projection `saved` response.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event Store `100`,
  Scheduler `20`, Pi Loop Kernel `27 passed / 2 skipped`, Harness service `36 passed / 1
  skipped`, Trace `13`, Eval `10 passed / 1 skipped`.
- `npm run harness:v2:typecheck`: passed.
- `npm run build --workspace=@anna/harness-service`: passed; emitted the sidecar and
  standalone approval bridge bundles.
- `git diff --check`: passed. No commit, push, merge, deploy, or release was performed.

## Remaining Goal Gates After Create Activation Slice

1. Create activation is locally executable only with explicit Owner bridge configuration;
   the Desktop Home caller is still Legacy until UI differential/activation contract
   acceptance is complete.
2. Cowork and Hub still require domain connector/run-profile contracts and caller cutover;
   generic injected Runtime tests are not production evidence.
3. T07 still requires the real provider, clean source checkout, backend, human Owner
   interaction, patch/screenshot/test evidence, and verified live manifest.
4. WebSearch external live evidence and external-provider Pi transcript restore remain
   operator gates.
5. Prime long-poll/tailing telemetry remains P2; controlled `from_seq` attach/reconnect,
   child attribution, and Harness-state projection are complete and tested.

## 2026-08-23 Local Durable Owner Bridge and Prime Status Correction

- Added `apps/harness-service/src/review-approval.ts`: a loopback-only Owner HTTP bridge with
  durable JSONL request/decision records, SHA-256 request fingerprints, owner header/body
  binding, pending-request inspection, explicit operator decision, timeout, and restart
  recovery. Unknown owners and malformed subjects fail closed; the bridge never auto-approves.
- Added the standalone compiled entry `apps/harness-service/dist/review-approval-main.js`
  through `npm run harness:v2:approval-bridge`. It can run without model configuration and
  is the local bridge to provide as `ANNA_T07_LIVE_APPROVAL_ORIGIN`.
- `probeReviewGate()` and the T07 preflight now require `decision_endpoint=ready` and
  `durability=durable`, in addition to the configured owner identity. The HTTP provider sends
  `x-anna-owner-id` and validates the returned `actorId`.
- Manual HTTP verification completed: `/status` returned ready; a real `/decisions` request
  became a durable pending request; `/requests/<id>/decision` returned an explicit Owner
  approval; after process restart the same fingerprint returned the persisted approval with
  HTTP 200. This is local bridge evidence, not production T07 evidence.
- Prime child Run attribution and Harness-state projection are implemented and tested, not
  post-MVP: parent/child/lane fields survive the durable command, Trace attribution, and
  HTTP `harnessState` family projection. The remaining Prime item is the intentionally P2
  long-poll/tailing telemetry; controlled `from_seq` attach/reconnect remains the MVP path.

## Verification After Local Durable Owner Bridge

- Focused Owner bridge and probe tests: `5 passed`.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event Store `100`,
  Scheduler `20`, Pi Loop Kernel `27 passed / 2 skipped`, Harness service `35 passed / 1
  skipped`, Trace `13`, Eval `10 passed / 1 skipped`.
- `npm run harness:v2:typecheck`: passed for all Harness v2 workspaces.
- `npm run build --workspace=@anna/harness-service`: passed; emitted both `dist/main.js` and
  `dist/review-approval-main.js`.
- `git diff --check`: passed. No commit, push, merge, deploy, or release was performed.

## Remaining Goal Gates After Local Durable Owner Bridge

1. Production T07 still requires a real provider Run, clean source checkout, backend origin,
   human Owner interaction, isolated patch/screenshot/test evidence, and verified manifest.
2. Create activation and the Create/Cowork/Hub Desktop caller migrations remain pending; the
   existing Legacy 409 boundary is preserved.
3. WebSearch has a bounded provider adapter but no external live evidence.
4. External-provider Pi transcript restore remains an operator canary and is skipped without
   explicit provider input.
5. Signed Desktop acceptance remains unverified without a valid Developer ID identity.

## 2026-08-23 Create v2 Minimal Vertical Slice

- Added the Create domain adapter at
  `apps/harness-service/src/create-artifact.ts` and the deterministic event
  projection at `apps/harness-service/src/create-projection.ts`.
- The v2 Create profile is `skills/harness-v2/create-assistant/SKILL.md` and
  exposes only the typed `create_artifact` Tool. Tool execution still crosses
  Anna ToolGateway; valid Skill content is normalized, validated, written under
  the bounded workspace `create-runs/<run>/skill/<skill_id>/SKILL.md`, and
  returned with a SHA-256 hash.
- Pi now persists `create.artifact.created` and `create.artifact.validated`
  after a successful `create_artifact` Tool result. A completed Run with a valid
  artifact projects to `ready_for_review`; only an explicit future
  `create.artifact.activated` event projects to `saved`.
- Added read-only `GET /v2/runs/:runId/create` with workspace/channel scope and
  explicit `POST /v2/runs/:runId/create/activate` returning HTTP 409
  `create_activation_not_implemented`. No activation or external write is
  claimed.
- Live Runtime now advertises only `create=available`; `cowork` and `hub` stay
  `unsupported` unless an explicitly injected Runtime enables them. The
  previous generic live sidecar Run evidence remains a historical Runtime
  boundary artifact and is not Create domain evidence. Explicit live Runtime
  configuration for `cowork` or `hub` is rejected rather than widening this
  boundary.
- Desktop now has `readHarnessV2CreateRun()` and
  `activateHarnessV2CreateRun()` on the explicit `v2ApiBase` seam. The Home
  Create caller remains on Legacy until activation parity and differential UI
  contract tests are accepted; no fallback was added.

## Verification After Create v2 Vertical Slice

- Red/Green: Create projection `6` tests; scoped Create API and activation
  boundary `2` tests; artifact adapter `2` tests; Pi Create Tool projection `1`
  test; live capability boundary `2` tests; Desktop Create API boundary included
  in `5/5` Harness v2 Desktop API tests.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event
  Store `100`, Scheduler `20`, Pi `27 passed / 2 skipped`, Harness service
  `33 passed / 1 skipped`, Trace `13`, Eval `10 passed / 1 skipped`.
- `npm exec --no -- vitest run`: passed, `55 files / 642 tests`.
- `npm run typecheck`: passed for root and all Harness v2 workspaces.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `npm run desktop:package`: passed and included the Create Skill in the
  packaged ASAR; signing skipped because the machine has zero valid Developer ID
  identities.
- `npm run desktop:smoke-asar`: passed with `servedIndex=true`, API health `ok`,
  and packaged model/MCP status explicitly `not_configured`.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, `17/17`.
- `git diff --check`: passed. No commit, push, merge, deploy, or release was
  performed; the existing dirty worktree remains protected.

## Create Slice Review and Remaining Gates

- Standards review: no P0/P1 found in the changed Create slice. The adapter is
  bounded to Skill artifacts, uses the Event Store projection as the fact source,
  and does not expose an activation claim.
- Spec review: this is a partial Create replacement, not T08 cutover. Legacy
  Create still owns the Home UI's SSE/draft/activation behavior; activation,
  Cowork, Hub, real Owner approval, real T07 provider/patch/screenshot/test
  evidence, and live WebSearch evidence remain open.
- The formal `code-review` Skill flow could not be invoked because the required
  `docs/agents/issue-tracker.md` is absent in this checkout and no formal review
  subagent interface is available. The manual two-axis review above is recorded
  explicitly rather than presenting it as an independent reviewer result.

## 2026-08-23 v2 Sidecar Client Boundary and Live Runbook

- `apps/desktop/src/lib/api/harnessV2.ts` now sends capabilities, canonical Run starts,
  and durable event reads to the explicit Electron-injected `v2ApiBase` and the Node
  sidecar routes (`/capabilities`, `/v2/surfaces/*/runs`, `/v2/runs/*/events`). It sends
  the existing workspace/user identity headers and fails closed before `fetch` when the
  sidecar base is absent.
- `apps/desktop/src/lib/api/harnessV2.test.ts` covers the independent sidecar origin,
  identity headers, cursor query, 409 behavior without Legacy retry, and missing-base
  fail-closed behavior. GREEN: `4/4`.
- `T07-live-runbook-2026-08-23.md` records the exact real inputs for source/head,
  backend/provider, Owner `/status` + `/decisions`, owner identity, and disposable
  evidence output. It uses the actual manifest verifier command.
- Create/Cowork/Hub production caller cutover remains intentionally unclaimed. The
  existing Create UI still consumes the Legacy CreateDraft SSE/artifact/activation
  contract; the generic v2 Run currently has no equivalent Create artifact projection,
  so switching the caller now would lose product state and violate the migration rule.

## Verification After v2 Sidecar Client Boundary

- `npm exec --no -- vitest run apps/desktop/src/lib/api/harnessV2.test.ts`: passed, `4/4`.
- No commit, push, merge, deploy, or release was performed; the dirty worktree remains
  protected.

## Final Verification After v2 Sidecar Client Boundary

- `npm exec --no -- vitest run`: passed, `55 files / 641 tests`.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event Store
  `100`, Scheduler `20`, Pi Loop `26 passed / 2 skipped`, Harness service `21 passed / 1
  skipped`, Trace `13`, Eval `10 passed / 1 skipped`.
- `npm run typecheck`: passed for root and all Harness v2 workspaces.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, `17/17`.
- `npm run desktop:package`: passed and rebuilt the latest frontend and sidecar. Code
  signing remains skipped because the machine has zero valid Developer ID identities.
- `npm run desktop:smoke-asar`: passed with `servedIndex=true`, API health `ok`, and
  packaged model/MCP status explicitly `not_configured`.
- `npm run live:t07`: returned `2` with
  `t07_live_operator_input_required` and the seven required real inputs. No production
  T07 success was claimed.
- `git diff --check`: passed; no test/build processes remain. No commit, push, merge,
  deploy, or release was performed.

## Next Executable Gates

1. Supply the real Owner/backend/provider inputs in `T07-live-runbook-2026-08-23.md` and
   run `npm run live:t07`; exit `0` is required for the real T07 production canary.
2. Freeze the Create domain output contract (artifact kind, validation, activation and
   UI frame mapping), implement it in v2 with differential tests, then switch the
   caller. Repeat the same sequence for Cowork and Hub; do not delete Legacy code before
   each surface has its own accepted replacement.
3. Configure an external WebSearch endpoint and preserve a live evidence summary that
   includes provider identity, bounded results and the Tool event chain.

## 2026-08-23 Prime Child Attribution and Harness-State Projection

- The existing durable `parentRunId` / `parentEventId` / `laneId` command contract and
  `projectRunFamilyHarnessState()` implementation were connected to the Review Trace HTTP
  response as `harnessState`.
- `packages/trace/src/index.ts` now includes events whose payload identifies the current
  Run as `parentRunId`, preserves child `run.started` / terminal events as attribution
  events, and closes the parent Trace only on the parent stream's terminal event. This
  prevents a child terminal from truncating the parent Trace.
- Added a RED/GREEN regression in
  `packages/trace/test/trace-projection.test.ts`: child events retain
  `parentRunId`, `parentEventId`, and `laneId` attribution while the parent remains open.
- Extended `apps/harness-service/test/review-api.test.ts` with a parent plus child stream;
  the HTTP response now proves two cursors, child Trace attribution, and durable family
  state for both the queued/running/terminal lifecycle fields.
- Desktop Review API types and Inspector now expose the returned `harnessState` alongside
  the Trace document. No separate in-memory lineage model was introduced.

## Final Verification After Child Attribution Slice

- `npm exec --no -- vitest run`: passed, `55 files / 640 tests`.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event Store `100`,
  Scheduler `20`, Pi Loop `26 passed / 2 skipped`, Harness service `21 passed / 1 skipped`,
  Trace `13`, Eval `10 passed / 1 skipped`.
- `npm run typecheck`: passed for root and all Harness v2 workspaces.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, `17/17`.
- `npm run desktop:package`: passed after the child attribution change and emitted the
  latest sidecar; code signing skipped because there are zero valid Developer ID identities.
- `npm run desktop:smoke-asar`: passed with `servedIndex=true`, API health `ok`, and
  model/MCP explicitly `not_configured` in the no-secret smoke environment.
- `./.venv/bin/python -m pytest -q`: the full Python suite remains `1048 passed, 53 warnings`;
  this Goal slice changed TypeScript only.
- `git diff --check` passed; no commit, push, merge, deploy, or release was performed.

## Remaining Goal Gates After Child Attribution Slice

1. T07 Review-to-Validated-Patch still requires a real Owner `/status` + `/decisions`
   service, human approval, isolated patch/screenshot/test evidence, and production gate
   acceptance.
2. Create/Cowork/Hub production caller cutover and domain Legacy migration remain pending;
   their 409 compatibility boundary is intentionally preserved.
3. WebSearch has a bounded provider contract but no external live WebSearch evidence.
4. Prime SSE remains controlled cursor attach/reconnect; long-poll/tailing telemetry is not
   implemented. Broad caller migration remains outside this slice.
5. Signed Desktop acceptance remains unverified without a valid Developer ID identity.

## 2026-08-23 Desktop Home Create v2 Caller Slice

- `apps/desktop/src/lib/api/harnessV2.ts` now provides the bounded Create subscriber:
  it attaches to canonical Event Store events, maps durable `seq=0` to the existing
  Desktop UI `seq=1`, converts reconnect cursors to `from_seq=uiSeq-1`, and closes on
  the durable Create projection. Abort and terminal error paths are explicit.
- `apps/desktop/src/pages/home/HomePage.tsx` now opts into v2 Create when the Electron
  runtime explicitly supplies `v2ApiBase`. It checks the `create` capability, sends
  workspace/channel/command/source attribution, reads the v2 projection after
  completion, and calls the v2 activation boundary. A v2 failure never retries through
  Legacy. With no v2 sidecar configured, the existing Legacy Create path remains the
  default. This caller slice currently accepts only `skill` artifacts.
- The v2 Home caller does not claim full Create migration: history/sidebar listing still
  uses the Legacy draft list because no v2 list projection endpoint exists; Cowork/Hub,
  real Owner approval, and T07 production canary remain separate gates.

## Verification After Desktop Home Create v2 Caller Slice

- RED/GREEN: the new subscriber tests first failed with the missing helper (`2` RED
  failures), then passed after implementation; focused Desktop API tests are `7/7`.
- `npm run build`: passed.
- `npm run harness:v2:build`: passed for both service and approval bridge entries.
- `npm run typecheck`: passed for root and all Harness v2 workspaces.
- `npm run harness:v2:test`: passed; Harness v2 `100 passed / 3 skipped`, Event Store
  `100`, Scheduler `20`, Pi Loop `27 passed / 2 skipped`, Harness service `36 passed / 1
  skipped`, Trace `13`, Eval `10 passed / 1 skipped`.
- `npm exec --no -- vitest run`: passed, `55 files / 644 tests`.
- `./.venv/bin/python -m pytest -q`: passed, `1048 passed, 53 warnings`.
- `node --test tests/frontend/electron_runtime.test.mjs`: passed, `17/17`.
- `npm run evidence:verify -- evals/harness-v2/t07-fixture-001/manifest.json`: passed,
  `4` files.
- `npm run desktop:package`: passed for macOS arm64. Signing remains skipped because
  the machine has zero valid Developer ID identities.
- `npm run desktop:smoke-asar`: passed with `servedIndex=true`, API health `ok`, the
  packaged Python runtime path, and model/MCP status explicitly `not_configured`.
- `git diff --check`: passed. No commit, push, merge, deploy, or release was performed;
  the dirty worktree remains protected.

## Remaining Goal Gates After Desktop Create v2 Caller Slice

1. Run `npm run live:t07` with the seven real operator inputs in
   `T07-live-runbook-2026-08-23.md`; exit `0` is required for the actual production
   Review-to-Validated-Patch canary.
2. Complete the v2 Create domain list/activation contract and differential tests before
   removing the remaining Legacy Create paths; then repeat the same migration sequence
   for Cowork and Hub. Their current `409 unsupported` boundary remains intentional.
3. Configure an external WebSearch provider and capture live provider, bounded-result,
   Tool-event, and evidence-manifest data.
4. Prime remains on controlled cursor attach/reconnect; long-poll/tailing telemetry is
   still P2. Signed Desktop acceptance still requires a valid Developer ID identity.

## 2026-08-23 Create Projection List and Desktop Read Cutover

- `StartRun.surfaceId` is now an optional persisted contract field. The v2 Runtime sets
  it from the requested surface, while legacy and existing scheduler commands remain
  valid without it.
- Both InMemory and SQLite scoped Event Stores now expose de-duplicated
  `listRunCommands()`. The service adds `GET /v2/create/runs`, scope-checks
  `workspace_id` and `channel_id`, filters `surfaceId=create`, and returns the durable
  Create projection plus the original prompt/command/source attribution. The Python
  managed bridge forwards the same list contract at `/api/harness/v2/create/runs`.
- Home Create history-open and Hub Create data now use the v2 list when an explicit
  `v2ApiBase` is configured; without the sidecar they retain the existing Legacy list.
  The Hub page read path is migrated, but the Hub execution surface remains explicitly
  unsupported. Cowork and Hub Run creation are still `409 unsupported`.

## Verification After Create Projection List Cutover

- RED/GREEN: the missing Desktop list helper first failed (`1` focused failure), then
  passed with Desktop API `8/8`.
- Event Store InMemory + SQLite conformance: `56/56` focused tests passed.
- Harness service Create projection tests: `10/10` focused tests passed.
- Python bridge test: `1 passed`.
- Full `npm exec --no -- vitest run`: `55 files / 645 tests` passed.
- Full `npm run harness:v2:test`: Harness v2 `100 passed / 3 skipped`, Event Store `102`,
  Scheduler `20`, Pi Loop `27 passed / 2 skipped`, Harness service `37 passed / 1 skipped`,
  Trace `13`, Eval `10 passed / 1 skipped`.
- `npm run typecheck`, `npm run build`, and `npm run harness:v2:build`: passed.
- Full Python suite: `1048 passed, 53 warnings`; Electron runtime: `17/17`.
- `npm run desktop:package`: passed for macOS arm64; signing skipped because there are
  zero valid Developer ID identities.
- `npm run desktop:smoke-asar`: passed with `servedIndex=true`, API health `ok`, packaged
  Python runtime resolved, and model/MCP `not_configured`.
- `git diff --check`: passed. No commit, push, merge, deploy, or release was performed;
  the dirty worktree remains protected.

## Remaining Goal Gates After Create Projection List Cutover

1. Execute `npm run live:t07` with the seven real inputs in
   `T07-live-runbook-2026-08-23.md`; only exit `0` can close the production
   Review-to-Validated-Patch gate.
2. Complete the real production Owner/provider bridge and capture live WebSearch evidence;
   local durable approval and external Chat/Pi/Desktop bridge runs are not T07 production
   evidence.
3. Freeze and implement Cowork and Hub execution contracts with real provider evidence;
   keep their current `409 unsupported` response until each surface passes its own live
   gate. Prime long-poll/tailing telemetry remains P2.
4. Signed Desktop acceptance still requires a valid Developer ID identity.

## 2026-08-23 Desktop Create Attribution Boundary Slice

- Fixed a T08 cutover gap in `apps/desktop/src/pages/home/HomePage.tsx`: when the
  v2 sidecar is configured, Create history readback, terminal projection readback,
  and activation now share `resolveCreateRuntimeBoundary`.
- A v2 Create operation must carry both the current Run ID attribution and its
  channel ID. Missing or mismatched attribution returns an explicit UI error; it
  cannot call `listDrafts()` or `activateDraft()` as a silent Legacy fallback.
- Legacy remains available only when the v2 sidecar is absent. This preserves the
  local-preview migration boundary without claiming T08 Legacy deletion or domain
  workflow completion.
- Added RED/GREEN unit coverage for Legacy mode, valid v2 attribution, missing
  attribution, mismatched Run IDs, and a source-level migration guard for Home.

## Verification After Desktop Create Attribution Boundary Slice

- RED: boundary test initially failed because the resolver did not exist.
- GREEN: boundary and Harness v2 Desktop API tests passed `14/14`.
- `npm run typecheck` passed; root Vitest passed `56 files / 651 tests`; frontend
  `npm run build` passed; `git diff --check` passed.
- No real provider, Owner bridge, external live canary, signed identity, or
  Cowork-domain/Hub-artifact production evidence was inferred from this slice.

## Remaining Goal Gates After Desktop Create Attribution Boundary Slice

1. Run `npm run live:websearch` with all six real inputs and a real provider; require
   exit `0` plus a redacted, hash-verified manifest.
2. Run `npm run live:t07` with all seven inputs from
   `T07-live-runbook-2026-08-23.md`; only exit `0` closes the production gate.
3. Define and independently validate Cowork domain workflows and Hub artifact
   projections before migrating their Legacy routes or claiming T08 complete.
4. Install a valid Developer ID identity for signed Desktop acceptance.

## 2026-08-23 Evidence Manifest Aggregate Gate

- Added `npm run evidence:verify:all`, which recursively discovers every
  `manifest.json` below `evals/harness-v2` and delegates each file to the existing
  strict verifier. A case is accepted only when all listed files still exist with
  the recorded size and SHA-256, no unlisted files were added, and no symlink is
  present in the case tree.
- Added `scripts/verify-evidence-manifests.test.mjs` covering both the all-pass
  aggregate and the fail-closed hash-mismatch path. This validates the audit
  mechanism itself without treating generated fixture evidence as live evidence.

## Verification After Evidence Manifest Aggregate Gate

- Focused Node tests: `2/2` passed.
- `npm run evidence:verify:all`: passed for all `7` manifests under
  `evals/harness-v2` (`32` files total).
- Current evidence remains separated by manifest metadata and directory naming:
  fixture/eval cases are reproducibility assets; `live-*` and `t07-*` directories
  are evidence containers only. A passing hash gate does not prove a real provider,
  Owner service, clean source checkout, or production canary.
- Historical sections above may describe an earlier unsupported state. The latest
  Cowork/Hub Runtime, explicit resume, WebSearch reachability, and aggregate evidence
  sections are the current state at this fixed point.

## Remaining Goal Gates After Evidence Manifest Aggregate Gate

1. Run `npm run live:websearch` with the six real operator inputs and a real
   WebSearch provider; require exit `0`, redacted provider/tool evidence, and a
   verified manifest.
2. Run `npm run live:t07` with the seven inputs in
   `T07-live-runbook-2026-08-23.md`; only exit `0` closes the production
   Review-to-Validated-Patch gate.
3. Independently define and validate Cowork domain workflows and Hub artifact
   projections before removing Legacy paths or claiming those product surfaces.
   Prime controlled cursor attach/reconnect is implemented; long-poll/tailing
   telemetry remains P2.
4. Signed Desktop acceptance still requires a valid Developer ID identity.

## 2026-08-23 Cowork Hub Runtime and Explicit Resume Slice

- `createLiveHarnessV2Runtime` now enables `create`, `cowork`, and `hub` by default.
  Cowork and Hub use the existing bounded general Skill and durable Runtime contract:
  channel-scoped Run claim, canonical events, ToolGateway restrictions, Contract Eval,
  terminal state, and cursor reads are shared with Create.
- This is a generic v2 Run surface. It does not implement Cowork domain workflows,
  Hub artifact projections, external business writes, or T08 Legacy deletion. The
  existing Python Legacy routes remain unchanged and are not silently used as fallback.
- Added explicit resume at
  `POST /v2/surfaces/<surface>/runs/<run>/resume`, with the matching Python managed
  bridge route and Desktop helper. Resume reads the original `StartRun` and exact
  `RunProfile` snapshot from the scoped Event Store, so a process restart cannot
  silently replace the model, Skill, Tool policy, budget, or surface contract.
- Resume remains surface-scoped and requires the requested workspace/channel scope;
  terminal Runs stay sealed. Missing Runtime resume support returns an explicit `409`.

## Verification After Cowork Hub and Resume Slice

- RED/GREEN: live Runtime initially rejected non-Create surfaces; the capability and
  explicit Cowork/Hub selection tests now pass.
- RED/GREEN: Node resume route initially returned `404`; the forwarding test and the
  SQLite-close/reopen HTTP integration test now pass. The integration test also caught
  and fixed a command-snapshot mismatch that had returned `500`.
- RED/GREEN: Desktop resume helper initially was missing, then API tests passed `9/9`.
  Python managed bridge resume initially returned `404`, then `1 passed` with the
  upstream status code preserved.
- Full verification passed: TypeScript typecheck; root Vitest `55 files / 646 tests`;
  Harness v2 `100 passed / 3 skipped`, Event Store `102`, Scheduler `20`, Pi Loop
  `27 passed / 2 skipped`, Harness service `40 passed / 1 skipped`, Trace `13`, Eval
  `10 passed / 1 skipped`; Python `1048 passed / 53 warnings`; frontend build.
- `npm run live:t07` and `npm run live:websearch` both returned exit `2` with their
  exact required operator inputs missing. The local runtime config has no WebSearch
  endpoint, so no real WebSearch provider evidence or T07 production canary is claimed.
- macOS arm64 `desktop:package` and `desktop:smoke-asar` passed. Smoke reported
  `servedIndex=true`, API health `ok`, packaged Python runtime resolved, and
  model/MCP `not_configured`. Signing remains skipped because no Developer ID identity
  is installed. `git diff --check` passed.
- Formal parallel Standards/Spec sub-agent review was unavailable because
  `docs/agents/issue-tracker.md` is absent and no sub-agent dispatcher is exposed;
  a manual two-axis review found no new P0/P1 issue in this slice. No commit, push,
  merge, deploy, or release was performed.

## Remaining Goal Gates After Cowork Hub and Resume Slice

1. Provide a real WebSearch endpoint/provider and run `npm run live:websearch` to
   produce exit `0` plus a verified redacted manifest.
2. Provide the seven T07 inputs, including a clean source checkout and real Owner
   `/status` and `/decisions` service, then run `npm run live:t07`; only exit `0`
   closes the production Review-to-Validated-Patch gate.
3. Implement and independently validate Cowork domain workflows and Hub artifact
   projections before claiming those product surfaces or deleting their Legacy paths.
   Prime controlled cursor reconnect is implemented; long-poll/tailing telemetry remains
   P2.
4. Signed Desktop acceptance still requires a valid Developer ID identity.

## 2026-08-23 WebSearch Live Canary Runner

- Added `scripts/live-web-search-e2e.mjs` and the root command `npm run live:websearch`.
- The runner requires six explicit operator inputs: backend origin, provider id,
  workspace id, channel id, query, and evidence directory. It preflights the direct
  Harness v2 service and requires the resolved WebSearch capability to be available.
- It starts a durable Create Run with exactly one `web_search` requirement, polls the
  scoped event cursor, and writes only redacted `preflight.json` and `summary.json`
  plus the generated evidence manifest. The query is represented by SHA-256 and length;
  raw query, provider output, and tool payloads are not written by the runner.
- Exit `0` requires a contiguous terminal event sequence, a successful WebSearch tool
  event, Create artifact creation and validation, a passing contract Eval, and a
  verified manifest. Missing inputs, unavailable capability, run failure, or incomplete
  evidence return exit `2`.
- `scripts/live-web-search-e2e.test.mjs` covers missing-input gating, redacted summary
  output, and a local fixture backend. The fixture test is harness validation only and
  is not live provider evidence.

## Verification After WebSearch Live Canary Runner

- RED/GREEN: the new runner test first failed with `ERR_MODULE_NOT_FOUND`, then passed
  `3/3` after implementation.
- `npm run live:websearch` without operator inputs returned exit `2` and listed all six
  required variables; no provider token was requested or emitted by the runner.
- `npm run typecheck`, root Vitest `55 files / 645 tests`, Harness v2 package suites,
  Python `1048 passed / 53 warnings`, and `npm run build` passed.
- `npm run desktop:package` passed for macOS arm64. `npm run desktop:smoke-asar` passed
  with `servedIndex=true`, API health `ok`, packaged Python runtime resolved, and
  model/MCP `not_configured`. Signing remains skipped because no Developer ID identity
  is installed.
- `git diff --check` passed. No commit, push, merge, deploy, or release was performed;
  the dirty worktree remains protected.

## Remaining Goal Gates After WebSearch Live Canary Runner

1. Run `npm run live:websearch` with the six real operator inputs and a real provider;
   exit `0` plus a verified manifest is the WebSearch live canary evidence.
2. Run `npm run live:t07` with the seven real inputs in `T07-live-runbook-2026-08-23.md`;
   only exit `0` can close the production Review-to-Validated-Patch gate.
3. Cowork and Hub execution remain `409 unsupported` until their contracts, provider
   wiring, and independent live evidence are complete. Prime long-poll/tailing telemetry
   remains P2; controlled cursor attach/reconnect is the current MVP path.
4. Signed Desktop acceptance still requires a valid Developer ID identity.

## 2026-08-23 WebSearch Tool Reachability Slice

- The audit found a capability mismatch: a configured WebSearch provider was reported
  as available, while the live Create Run profile exposed only `create_artifact`.
- `createLiveHarnessV2Runtime` now passes the provider configuration into the Create
  profile. The Create Skill declares `web_search`, while the resolved profile still
  removes it when no provider is configured. The existing bounded ToolGateway remains
  the only execution path and normalizes at most five results.
- This closes the local profile/Tool reachability contract. It does not create live
  provider evidence; the provider endpoint, returned sources, Tool events and a
  manifest still require an operator-run external provider canary.

## Verification After WebSearch Tool Reachability Slice

- RED: the new profile contract failed because `createLiveProfile` was not exported,
  then failed because Skill resolution correctly removed undeclared `web_search`.
- GREEN: Harness service WebSearch tests passed `4/4`; Harness service full tests passed
  `38 passed / 1 skipped`; Pi kernel focused tests passed `25/25`.
- Full verification passed: TypeScript typecheck, root Vitest `55 files / 645 tests`,
  Harness v2 package suites, Python `1048 passed / 53 warnings`, and frontend build.
- All seven existing evidence manifests passed `evidence:verify`. `npm run live:t07`
  returned exit `2` with all seven real operator inputs missing, so no production T07
  result was claimed.
- `harness:v2:build`, macOS arm64 `desktop:package`, and `desktop:smoke-asar` passed.
  Smoke reported `servedIndex=true`, health `ok`, packaged Python runtime resolved,
  and model/MCP `not_configured`. Signing remained skipped because no Developer ID
  identity is installed.
- `git diff --check` passed. No commit, push, merge, deploy, or release was performed;
  the dirty worktree remains protected.

## Remaining Goal Gates After WebSearch Tool Reachability Slice

1. Run `npm run live:t07` with the seven real inputs in `T07-live-runbook-2026-08-23.md`;
   only exit `0` can close the production Review-to-Validated-Patch gate.
2. Execute a real WebSearch provider canary and retain redacted provider, bounded-result,
   Tool-event and manifest evidence. Profile wiring and fixture provider tests do not
   satisfy this gate.
3. Cowork and Hub execution remain `409 unsupported` until their contracts, provider
   wiring and independent live evidence are complete. Prime long-poll/tailing telemetry
   remains P2; controlled cursor attach/reconnect is the current MVP path.
4. Signed Desktop acceptance still requires a valid Developer ID identity.

## Current Fixed-Point Supersession Note

- The latest Desktop Create state is the attribution-boundary slice above: v2-configured
  Home flows cannot silently call Legacy Create readback or activation. The generic
  Cowork/Hub v2 Runtime and explicit resume implementation described above remain
  generic contract coverage only; they do not close Cowork domain or Hub artifact gates.
- Evidence manifests are currently hash-valid, but `npm run live:t07` and
  `npm run live:websearch` still require real operator inputs and external services.
  No fixture, local bridge, or injected Runtime result is production evidence.

## 2026-08-23 Desktop Runtime Crash Recovery Slice

- T08 crash recovery is now explicit in the Electron failure page: an unexpected
  Python or managed Harness v2 child exit remains visible, and the page can invoke
  the existing `anna:restart-runtime` IPC. A successful restart navigates back to the
  app URL; a failed restart stays visible with a retry action and redacted text.
- Restart preserves the current API and Harness v2 ports. When the current runtime
  owns a managed v2 sidecar, restart removes the resolved internal bridge origin so
  the sidecar is spawned again as managed rather than accidentally treated as an
  external bridge.
- Added `parseRuntimeBaseFromArgv` and wired the existing preload call to it. This
  fixes the missing v2 runtime injection path used by Desktop Home and restart flow.
- This closes the local T08 crash/restart seam only. It does not close the T07 real
  provider/Owner/live-canary gates or authorize Legacy deletion.

## Verification After Desktop Runtime Crash Recovery Slice

- RED/GREEN: failure-page restart test, managed-sidecar restart test, and v2 argv
  parser test were added; Electron runtime suite passed `19/19`.
- The failure page preserves the existing secret-redaction test and exposes only
  `apiBase`/`appUrl` returned by the main-process restart handler.
- Follow-up audit found the preload caller was missing the new parser import;
  `preload.mjs` now imports `parseRuntimeBaseFromArgv`, with a source-level guard
  in the Electron runtime test.
- Remaining real WebSearch/T07 operator inputs are still required; no provider or
  production canary evidence was created by this local test slice.

## Verification After Preload Import Fix

- Electron runtime suite: `19 passed / 0 failed`.
- Root Vitest: `56 files / 651 tests passed`.
- Workspace regression: Harness v2 `100 passed / 3 skipped`, Event Store `102/102`,
  Scheduler `20/20`, Pi `27 passed / 2 skipped`, Harness service `40 passed / 1 skipped`,
  Trace `13/13`, Eval `10 passed / 1 skipped`.
- TypeScript typecheck passed. Evidence verification passed all `7` manifests and
  `32` referenced files. `git diff --check` passed.
- No commit, push, merge, deploy, or release was performed; the dirty worktree
  remains protected.
