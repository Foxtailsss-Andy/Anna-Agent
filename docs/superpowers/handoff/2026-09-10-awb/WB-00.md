# WB-00 交接胶囊

状态：WB-00 基线记录、职责设计及 AC-18a 已由 Astra 主控验收；WB-01 新主控已创建，owner 交接以 STATUS 的最终记录为准。产品能力失败/外部门禁继续保留。

## 范围与版本

- scope：M0 + M1；本 Session 仅 WB-00。下一张卡 WB-01，WB-05～09 不在授权范围。
- owner_task_id（本卡交付主控）：`01a08936-3b2d-7500-81b1-3407c5a172e4`；next_owner_task_id：`01a08965-d562-7340-ada4-8456ba110690`（local，gpt-6-astra / xhigh），接 WB-01。
- worktree：Anna 外层容器中的 `Anna-Workbench-Plan-20260907`；branch：`codex/workbench-plan-20260907`。
- pre-change / 首轮测试 HEAD：`d461a1a344d82d7bd95ca5073b0a1e2a700d2162`；本 Session 再次 `git ls-remote` 核对远端 main 相同。
- 集成代码提交：`7bee80dbaac4d77b56932e8deba4d79accdee401`。35 个代码/数据文件已逐一核对提交字节与独立审查清单相同；提交后完整性 CLI 再验证 5 pass。r5 在提交前的同一代码工作树运行，其 source_sha 为起点，dirty_diff_digest 为 `sha256:7febd05757f46eb7edf9e1ae22d8d5eccf0c563b75b39ad0a8c5a4d87cba0c38`。
- 交接文档提交后的最终 HEAD 见 STATUS 的交接记录；该后续提交只保存本胶囊、审查与 UI 证据，不改变执行源码。
- 产品 Spec：1.2；SHA256 `d3ec39733a170e25e1d34a72e1b4b0d456572001acd414ba191eaace43e159e7`。
- R-01/02/17/18/19；本卡只关闭基线记录、职责设计和 AC-18a，不关闭 AC-01～08、L/M/P 或最终 AC-18c。
- Anna package 0.2.0；Node 24.12.0 / Python 3.12.13；参考机 Apple M1 Pro，16 GiB，macOS arm64。
- 本树重新 materialize：OMP 18.0.11 / Bun 1.3.14；manifest `sha256:d3ff0284f9d7a4489d66e78fb5b1d3819471a59f4c9dcd01d0e118b6994a241d`，21,465 文件；主控已逐文件 verify。源码 worker/协议改变后重新生成，不能复用另一树的包。

## 已完成的工程切片

- 新增公共边界基线测试、固定评测数据集及 runner/validator；CI 绑定当前 round，不从历史轮补成功数。
- 新增真实 Product Host + model-less Python + 原 React UI 的观察脚本；数据/配置/端口隔离，Provider/MCP 缺失明确返回失败。
- 目标职责、实际源码触点及 WB-01～08 的可验证起点见 [职责记录](WB-00-responsibilities.md)。
- 两轴独立审查及修正状态见 [审查记录](WB-00-review.md)。原有产品源码、用户数据和业务规则未修改。

## 真实基线与未验收范围

| 层 | 观察与证据 | 结论边界 |
| --- | --- | --- |
| D | Create 普通问题通过原 UI 发出 `kind=skill`；SSE 有 `create.skill.run.created`、`create.failed(model_not_configured)` | 入口绑定为 fail，归 WB-02/03；不是 Skill 创建的实际质量尝试 |
| D | public resolveRunProfile 去掉选中 Skill 之外的工具；另扣除 forbiddenTools | 当前限制已复现，归 WB-02；不允许用 Skill 文字扩权 |
| D | OmpLoopKernel.answer 未实现；运行中 Product Host signal 返回 409 `harness_signal_unavailable` | 问答控制目标 fail，归 WB-04；等待后的幂等消费未验证 |
| D+O | 两个独立 Run 经真实 OMP worker 同时进入固定外部 transport barrier，并各自完成，事件类型/seq 已留存 | 只证明 Runtime 边界重叠；不证明产品 UI、第三项排队、公平性或真实模型并发 |
| D | Crew 内置合成案例的真实频道 POST 返回用户 say；当前无模型无法产出上下文回答 | L blocked；内置案例不能当现场模型结果 |
| D | Cowork Hiker 真实业务读 API 返回 failed / connector_not_configured | 真实 MCP 数值 M blocked；活跃 Run 同时读看板的产品组合场景 not_run |
| L/M | 固定 12 Case × 3 槽，当前 21 blocked / 15 not_run；0 pass / 0 fail | 缺外部条件，未造真实尝试；不应用最终 33/36 门槛 |
| P | 本卡未生成并验收桌面分发包 | not_run；留 WB-08，不能由当前 build/OMP materialize 替代 |

主控独立 UI 复跑证据：[脱敏 JSON](WB-00-product/observation.json) 与同目录三张原样 PNG；[manifest](WB-00-product/manifest.json) 校验通过。该轮 Host 与 business PID 均已另查不存在，浏览器已关闭。OMP manifest 读取与实际 worker 执行分开标注。

评测冻结轮：[r5](../../../../evals/workbench/wb00/runs/wb00-baseline-20260910-r5/evidence/baseline-result.json)，dataset `wb00-dataset-v1.1`；完整快照与摘要在同轮 evidence 内。旧 r3/r4 使用保留的 v1 数据集校验，原字节不变，不跨轮累积结果。运行时模型未配置；D/O 使用固定 transport，工程模型 Astra/Luna 不冒充产品模型。缺失 usage 为 unavailable。

后续固定输入装载：读取对应 Case 的 `inputFixture` 与完整 `fixture`，保留 fixtureId 和内容 hash。L-04 用 `project/tasks/artifacts/channel` 作为合成项目资料，延期事实为源字段清单未提供；L-05 将 `technicalProposal` 正文与 constraints 作为待评估方案，不能把其中的方案主张直接当已验证架构结论；L-06 比较 before=128000、after=141500 CNY，预期变化=13500，来源始终注明 synthetic；L-12 用 initialData 的 100/200 两行作为 v1，按 review.comment 追加 source 列并保持数值，保留 dependencies 与版本引用。

在后续产品测试中，通过对应已实现的公共项目/文件/频道/评审接口装载这些数据，记录 fixture ID 到实际业务 ID 的映射，不直接修改内部数据库。尚无装载接口时保持 blocked/not_run，不临时编造另一套材料。L-06 静态数据只服务 D 比较；真实 M 仍需获准合成业务记录与 MCP 读回，不得用内联 fixture 冒充。

## 验证记录

| 命令 | 结果 |
| --- | --- |
| `npm ci`；`uv python install 3.12`；`uv sync --locked --extra dev` | 各退出 0，本树依赖就绪 |
| `npm run harness:omp:prepare`；公共 `verifyRuntimeManifest` | 退出 0，锁定运行包匹配全部文件 |
| `npm run harness:v2:build`；`npm run build` | 退出 0 |
| `npm run typecheck` | 退出 0 |
| `./.venv/bin/python -m pytest -q` | 退出 0：1,079 passed，53 warnings |
| `npm test -- --reporter=dot` | 退出 1：1,172 passed，1 failed，7 skipped；详情见下 |
| `npm run test --workspace=@anna/harness-service -- --run test/omp-resume-budget.test.ts -t 'times out a completed-answer restore after the original wall budget'` | 退出 0：1 passed / 9 skipped；22.85 秒 |
| `npm run frontend:product-smoke` | 退出 0：9 项既有配置/源码/启动测试，不作为 UI 验收 |
| `node scripts/wb00-product-observation.mjs` | 主控独立复跑退出 0；原 UI 四项状态如上 |
| `npm run release:verify`；`git diff --check` | 退出 0 |
| `node --test scripts/workbench-baseline-integrity.test.mjs` | 主控独立退出 0：5 pass；旧 r3/r4 仍可核对 |
| `ANNA_WB00_EVAL_ROUND_ID=wb00-baseline-20260910-r5 npm run workbench:baseline` | 退出 0，5 项 OMP 聚焦测试通过，36 槽如上 |
| `ANNA_WB00_RUN_DIR=evals/workbench/wb00/runs/wb00-baseline-20260910-r5 npm run workbench:baseline:verify` | Coding、主控及两名独立 reviewer 均退出 0 |

全套 JS 的唯一失败是旧 `apps/harness-service/test/omp-resume-budget.test.ts:122`：期望 `simulated terminal persistence loss`，实际先收到 `OMP worker readiness timed out`。未改该测试或产品恢复源码；单独复跑通过。根因未定位，不能把全套结果改成绿灯。它是本轮基线的验证限制，恢复/预算后续验证需保留此用例；本卡不会通过放宽超时掩盖它。

完整本地日志保存在 `.tmp-tests/wb00/supervisor/`；首个 Create 目标 RED 在 `.tmp-tests/wb00/coding/red-create-profile.log`（退出 1）。原完整性漏洞的 RED 来自首轮独立源码/VM 审查；`.tmp-tests/wb00/integrity/` 的首次 CLI 失败是修正过程中 scoring 字段映射错误，随后 5 项 GREEN，来源不混淆。

CI 的新增 CLI integrity step 在 r5 生成后加入，故 r5 中该 CI 文件摘要早于最后配置，其他执行源码摘要一致。新增命令已独立运行通过；远端 CI 本卡仅配置，未推送/未触发，不声称远端通过。

## 接力与下一动作

已确定：模型决定行动；OMP 是实际起点；Session、Run、Project 与 Channel 各自保留语义；一个 Run 一个执行事实 owner；Surface/旧 API 不构成新架构上限。未采用 Codex SDK 迁移、双执行链或 Memory 自动扩展。

WB-01 首个切片从产品 Session/Run 提交查询、认证 scope 和持久化映射开始：同 conversation 的下一轮及跨显示入口延续、同 Project 多私聊隔离、冒充 actor/project 拒绝、v1 历史 dry-run 与幂等迁移。正式 API/存储由新主控按 R-03/15/17、AC-03/15/17a 推导；不把旧 ProductSessionStore 的名称当作已实现稳定 Session。

全部 Coding 与 Reviewer 已结束，已收回所有文件写入权。本卡测试、UI 服务与浏览器均已停止，无待接管的运行服务；历次 UI 观察的 10 个 Host/business PID 已检查均不存在。新 Astra xhigh 主控已创建；旧主控完成本文档提交后只写一次 STATUS 转交 owner，此后不再派工或修改。

预期交接 dirty 内容仅为启动前用户的 AGENTS.md、CONTEXT.md、docs/agents/ 与规划目录（其中 STATUS 为已授权进度更新）；这些文件未被夹带提交。AGENTS、CONTEXT、SPEC、三个依赖 lockfile 的原哈希均已核对一致。保留 `.tmp-tests/wb00/` 本地诊断日志及本树构建/依赖，不作为待发布产物。

保留内容 digest：`sha256:215ce49d45fe610150a8a2c2b6051667768f458f22b3ed117057737aad08c8b8`，范围为 tracked diff 摘要与 10 个原有未跟踪文档的逐文件摘要；有意排除可变 STATUS owner/进度。计算材料在 `.tmp-tests/wb00/supervisor/handoff-dirty.json`，无需在新 Session 重新生成规划文件。
