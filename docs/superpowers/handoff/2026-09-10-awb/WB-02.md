# WB-02 核心切片交接胶囊

状态：能力核心切片 A 的 D/O 契约验收通过，AC-18b 核心增量完成；整张 WB-02 未关闭。Skill 与其余目录能力继续由下一主控完成。最终 owner 与交接 HEAD 以 STATUS 为准。

## 范围与现场

- scope：M0 + M1；current_ticket：WB-02；current_slice：能力目录快照、按需工具加载、Crew 读取、权限与持久恢复。
- owner_task_id：`01a08a0b-f70a-7032-9f22-07e0df963ca5`；Astra xhigh 主控，Luna xhigh 实施，两轴 Astra xhigh 独立审查。
- worktree：Anna 外层容器内 `Anna-Workbench-Plan-20260907`；branch：`codex/workbench-plan-20260907`。
- pre-change HEAD：`eec76df8cc7fc40d412f5f3e253fd15d0a4c07b1`；本卡代码/证据提交：`dae2b4fde6a0efe01408e600bf04625fa55fa680`；交接文档提交暨交接 HEAD 见 STATUS。
- Spec 1.2 SHA256：`d3ec39733a170e25e1d34a72e1b4b0d456572001acd414ba191eaace43e159e7`；R-01/04/05/15，AC-01/04/15 对应核心部分及 AC-18b。AC-05 的公共来源/外部连接器仍未完成。
- 原 dirty：AGENTS、CONTEXT、docs/agents、AWB 规划。原 tracked diff SHA256 `5a5a75dad632d126dad8073c8548b44508804e4329dc52d54696697da6c4611b`；除已授权 STATUS 进度/owner，保留原字节，不整树 stage。
- Node 24.12.0 / Python 3.12.13；OMP 18.0.11 / Bun 1.3.14；macOS arm64。

## 实现与决定

公共契约见 [WB-02-contract](WB-02-contract.md)，来源与取舍见 [WB-02-design](WB-02-design.md)。

通过 WB-01 的真实 Workbench 提交链，v2 从小型控制工具集合开始；模型发现/加载 Crew 项目或频道读取，下一次实际 OMP 请求携带相应定义。两个真实 Project 的不同工具观察分别驱动 A→完成、A→B；chat/create/crew 同授权可达同类能力，无工具 Create 允许文本完成。

目录/版本/schema/来源/effect/hash 固定在 RunProfile 快照；Host Gateway 负责有效授权、严格 schema、事件和业务 I/O。load receipt 与真实模型调用/dispatch/成功响应关联，恢复按每轮合法加载前缀核对实际定义。实际业务撤权在目标 I/O 前拒绝，并验证另一正常 Run 继续完成。默认 Skill 硬绑定解除；显式 Skill 限制保留。v1 兼容按明确策略版本处理。

OMP 原生 discoverable/xd 机制已对锁定源码核对；当前 Anna 受管 Worker 限制使 xd 的 read/write 路径不适用。本切片使用公开 `agent.setTools`，没有整体打开 OMP 内置执行器/MCP/扩展。原生实现不是未经验证的默认替换方向。

历史目录测试在真实 Profile/StartRun、EventStore、Gateway 与 Python 业务边界完成，属于 D。其 schema 使用测试中的输入接口，不能单独证明生产旧版本 OMP schema 恢复；生产快照消费另经静态审查，常规 cold 恢复经实际 OMP 回归。恢复夹具是从真实执行捕获事件和 Host context projection 后，在隔离 store 重建检查点，不是 OS crash 测试。

## 审查、失败与验证

完整两轴结果见 [WB-02-review](WB-02-review.md)。证据 CLI 的原 Standards 1 / Spec 2 已各自清零；产品核心 r1 为 Standards 2 / Spec 4 / smell 1，r2 又发现 Standards 1 / Spec 2，均经修复。完整回归进一步发现旧 client fixture 与取消顺序回归，修复后的 r4 两轴均为 0。产品核心冻结为 22 文件，patch 294,957 bytes / SHA256 `5c830fadeab4dcc36eada77cd10bcd7ade47079d53ee0f58094e0b0bd3ec631c`。没有用测试总数代替具体契约证据。

随后全仓发布扫描发现三个既有测试文件的无效鉴权字符串/路径前缀断言误报；仅做等值字符串构造，扫描器和原断言保持。r5 合并范围为 25 文件，patch 298,124 bytes / SHA256 `86399f373f7f141038c3788357222a9737b3feb8cb3bdb09ad326a01b95dc48c`；两轴均 0，核心 22 文件字节不变。主控和两轴分别内联还原新常量后，三个整文件与入场 HEAD 逐字一致。

必须保留的历史限制：

- A2 组合轮 `final-capability-tests-004` 两例 120 秒超时，整轮 450.93 秒；具体慢层/根因未知。EventStore 省略游标与 -1 同义，不能声称该变更修复根因。后续通过是独立记录。
- 早期固定频道序列未证明 A→B，身份 mock 未证明真实权限，v1 旧 marker 未证明恢复后新读取，旧前缀负例的 digest 格式错误未证明前缀守卫。这些均已识别并保留原记录，不计最终行为通过。
- 恢复 fixture 的错误 resume 路径、缺 Host context projection、旧事件计入新 dispatch，以及错误 digest 格式/排序属于夹具问题。名为 green 的失败文件仍按实际退出码记账。
- r2 全套 JS 在独立复审发现剩余缺陷后由主控中止，137.6 秒、返回 -2/SIGINT；不是产品测试失败或通过。
- r3 初步 v1 GREEN 日志因 writer 重名被 hash GREEN 覆盖，原字节无法恢复；不参与验收。后续真实 scope 校正版 v1 RED/GREEN 与 hash RED/现存 GREEN 完整，详见审查记录。不能声称所有探索日志原字节都保留。
- r3 完整 JS 为 1,193 passed / 3 failed / 7 skipped，退出 1，864.96 秒。client fixture 已同步实际代码；取消顺序回归已修复。旧 tool budget 用例实际报 Worker readiness 超时，同源孤立复跑通过，具体慢层/根因仍未知，不能记为已修复。详见审查与本地原始日志。

| 检查 | 当前已核对记录 |
| --- | --- |
| Python 全套 | 1,085 passed / 53 warnings，38.77 秒，退出 0；后续仅 TS 修复 |
| 运行包完整性 | 全 21,465 文件通过；worker/protocol/runtime lock 与源码一致 |
| 产品入口源码 smoke | 9 passed，退出 0；只证明既有入口/启动源码检查，不是桌面 UI live |
| CLI guard | 4 passed，退出 0；证据编排的隔离 synthetic 命令测试 |
| Worker 与最终全仓 typecheck | Worker r4、全仓 r5 主控检查均退出 0；最终代码提交字节 |
| r4 Web/Host build | 主控均退出 0；最终冻结字节 |
| r4 最终完整 JS | 1,196 passed / 0 failed / 7 skipped，888.02 秒，退出 0；22/22 源码 hash 与冻结一致 |
| 正式核心增量 | `wb02-capability-core-20260910-r1`：4 TS 文件 / 14 tests、Python 5 tests 通过；独立 manifest 校验 15 文件通过；源无漂移 |
| r5 fixture 定向回归 | 三文件共 5 tests 通过；它们是 1,196 项中的已有用例，不另累加。此后未重跑全套 JS；产品核心 22 字节不变，三处字符串经独立等值核对 |
| 全仓发布边界 | 等值修正后主控 `npm run release:verify` 退出 0；未改扫描规则、未推送或发布 |

本地原始诊断与完整日志在 `.tmp-tests/wb02/`；公开交接只提交脱敏的确定性业务事实/摘要，不携带模型原文、配置或私密状态。

## 构建与评测身份

新 OMP materialization 的规范 manifest.sha256 为 `sha256:168da57a60493b094ea6b2525f312c3e05a6585ffc167cba2a7b9c5beb6719ec`，21,465 文件；旧运行包保留于本地 `.tmp-tests/wb02/runtime-before`。正式增量结束后的再次独立核验记录为 `supervisor/final-checks/r4-runtime-identity.json`；全部文件及 worker/protocol/锁文件仍匹配。

- worker SHA256：`1cf42eb0907170cfd1c198b5e0c990ff55cf3b741e02c8db6bbed13c395d91b8`。
- protocol SHA256：`f8130c5d1af12316b0875f1e0368a436a1e125e0c155fc3cb66a4d8d76fa2a56`。
- runtime lock SHA256：`f5a96c28a0187d549959fbe33c78596198491df29be9ea18424bf814fea88ceb`。
- manifest 文件字节 SHA256：`3a78ef5da9aa194186fdde8324633928a7d37f2a07ea4f6be64c9cb927fe0def`；与规范摘要口径不同。

最终冻结源的 Host 主 bundle 为 1,536,775 bytes，SHA256 `07cc24c5b61f4fa8efab61b52bd75ae0ab56bc28efcfc643e752a616620b8c54`；Web index 为 392 bytes，SHA256 `19f90c39b7939be84c373862ca6b266b3211e1cea8aa8abe5e9064df34c3dcf6`。原 Web 源码与产物保持，Host 已重新构建。构建和完整 JS 的前后源检查通过。

基线固定 WB-00 r5 / wb00-dataset-v1.1，36 槽仍 21 blocked / 15 not_run；不修改旧轮、不跨轮累加结果。usage unavailable，L/M blocked、P not_run，不补 0，不宣称全部 M1 或分发包验收。

## 最终核心增量与证据解释

[核心增量结果](../../../../evals/workbench/wb02/runs/wb02-capability-core-20260910-r1/evidence/increment-result.json)、[源码快照](../../../../evals/workbench/wb02/runs/wb02-capability-core-20260910-r1/evidence/source-snapshot.json)、[manifest](../../../../evals/workbench/wb02/runs/wb02-capability-core-20260910-r1/evidence/manifest.json) 均已生成并独立核验。该目录共 16 个可解析 JSON，manifest 校验其余 15 文件。结果中的 R/AC 是任务关联，不能据总 status=pass 关闭全部 AC-05 或 WB-02。

38 个关联源码/依赖/脚本 hash 与现场一致；sourceChanged=false、ownedChanged=false。模块摘要为 `sha256:481b04d1c6770a2361548c2758cc70c2942d51b2e975dd9221a961ba59a6cfac`；本轮 dirty digest 为 `sha256:a2b854edef08c82b9221be3bfad7f21930ae9327d8772e640125b1a6fe34ea2c`。生成时 HEAD 为 pre-change eec76df，执行的是冻结 dirty 源。主控已逐一核对上述 38 个源 hash 和最终审查的 25 个文件 hash，均与代码提交 `dae2b4fde6a0efe01408e600bf04625fa55fa680` 的 Git blob 一致；提交的 41 路径恰为 25 实现/测试/配置加 16 公开证据。不能把证据中的旧 HEAD 字段当作最终代码提交。

脚本顶层自动只记 D，O 为待主控核定的 metadata。主控根据真实 OMP Worker、Canonical EventStore 和实际断言，将以下范围核定为 D+O，保留模型 transport 为固定 fixture：

- loading 与两份 observation：先加载/读取 Project A；观察充分时完成，需要频道事实时加载/读取 B，并读到通过真实业务 API 写入的频道 marker。两种观察走不同动作序列。
- chat/create/crew 三份 scope：同授权目录、实际加载定义与业务事实可达一致。文本 Create 无工具/Todo/Artifact 用例由断言覆盖，未单独写一份完整 Trace。
- 两份 revocation：目标读取工具失败，正常 Run 的读取成功；整个两 Run fixture 的 Crew I/O 总数为 1。receipt 中的 `business_crew_tool_call_count` 是共享总数，不能解释为每个 Run 各调用一次；`target_business_io_reached` 是结合该总数、正常真实结果与拒绝事件得出的断言结论，不是独立网络 Trace。
- capability restore 与两个 v1 记录：实际加载恢复、恢复后新读取、真实 Project 的 v1 固定工具保持可用。restore receipt 的 `resumed_tool_names` 包含保留历史；真正恢复后的新 dispatch 由 seq 水位断言核对为一个 Project read，不能把列表三项都算成新调用。

主控另对公开摘要中具备完整字段的 26 次模型请求、7 条加载记录、4 条可用的 dispatch 关联做了一致性检查：加载前缀、名称/hash 数量、同 canonical ID 的摘要均一致。其他精简记录不具备全部字段，不称为完整 Trace。历史目录版本变化的 Gateway/Python 用例维持 D；缺模型定义、hash 错误、坏关联与前缀负例的拒绝由真实捕获检查点和恢复断言覆盖。

这些结果不评定真实模型自主判断质量、不证明真实 MCP 效果，也不替代 WB-03 的普通 UI 或分发包验收。

## 下一主控的具体工作

依据 SUPERVISOR，大卡形成独立可验证切片后可换 fresh Supervisor；下一主控继续 **WB-02 剩余切片**，不能直接进入 WB-03 或把本胶囊当作整卡完成。

1. 核对唯一 owner、实际工作树、提交、原 dirty、最终源码/运行包 hash；owner 未转交时仅只读等待。
2. 在当前公开 Workbench + 真实 OMP/Gateway/业务 scope 上，派 fresh Luna 从按需 Skill、固定连接器、公共搜索/URL/文件能力中选择一个最小垂直切片。先核对已有实现，再决定最少接线；正文不授予权限。
3. 补 R-05/AC-05 的实际来源/时间、截断后补证、缺配置/参数错误事实；维持 R-04 的同授权跨入口、snapshot/receipt/schema 一致性。文件能力先明确可信资源解析，不接受任意 renderer workdir/path 扩权。不要预做 WB-06 Sandbox。
4. 每一切片保留有效 RED/GREEN、独立两轴、AC-18b 新轮和失败记录；整 WB-02 达到对应退出条件后，再 fresh WB-03；最后 WB-04 集成控制。WB-05～09、push、PR、发布不在授权范围。

全部 Coding、Reviewer、验证/业务测试进程已完成并 drain；主控已收回写入权。代码提交后，12 个受保护输入保持原字节（STATUS 除外），原 tracked dirty SHA256 仍为 `5a5a75dad632d126dad8073c8548b44508804e4329dc52d54696697da6c4611b`。新主控创建后单次转交 STATUS，旧主控停止所有写入和派工。回退只用保留的已验收源码/运行包与独立状态快照，不清理用户数据或退回 Python Agent 执行。
