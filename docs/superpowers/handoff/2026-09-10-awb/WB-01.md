# WB-01 交接胶囊

状态：WB-01 契约验收通过。AC-03/15 对应 D/O 契约部分、AC-17a 与本卡 AC-18b 增量记录完成；真实 Provider/MCP 的 L/M 子项仍 blocked，P not_run。当前未宣称全部 M1 验收。

## 范围、所有权与版本

- scope：M0 + M1；本主控仅 WB-01。下一卡 WB-02；WB-05～09 不在当前授权范围。
- owner_task_id：`01a08965-d562-7340-ada4-8456ba110690`；下一 owner 与最终交接 HEAD 以 STATUS 为唯一来源。
- 实际工作树：Anna 外层容器内 `Anna-Workbench-Plan-20260907`；branch `codex/workbench-plan-20260907`。
- pre-change HEAD：`cfe0731700268fd466b588a65641c1d162487443`；本卡集成代码提交：`020112091849f0d3a2c08349ff38c4b13d44fff0`；交接文档提交：见 STATUS。
- Spec：1.2，SHA256 `d3ec39733a170e25e1d34a72e1b4b0d456572001acd414ba191eaace43e159e7`。
- R-03/15/17；AC-03/15 契约部分、AC-17a、AC-18b。本卡不关闭真实 Provider/MCP、全部 M1 或分发包验收。
- Node 24.12.0 / Python 3.12.13；OMP 18.0.11 / Bun 1.3.14；macOS arm64。
- OMP 运行包沿本树已核验 materialization，manifest.sha256：`d3ff0284f9d7a4489d66e78fb5b1d3819471a59f4c9dcd01d0e118b6994a241d`，21,465 文件。worker/协议/依赖未改；Host 按当前源重新 build。后续改 worker 必须重新 materialize，不复用别树包。
- 根 package-lock、uv.lock 与 runtime/package-lock 均保持交接输入；runtime lock SHA256 `f5a96c28a0187d549959fbe33c78596198491df29be9ea18424bf814fea88ceb`。

## 实现与事实所有权

正式公共契约见 [WB-01-contract](WB-01-contract.md)，设计与阶段边界见 [WB-01-design](WB-01-design.md)。

- Node 提供稳定 Session 创建/列表/详情、其下 Run 提交、Run/事件查询以及当前认证用户的旧状态迁移入口。
- 真实 Python IdentityService 解析 Bearer；无 Authorization 可用既有本地身份，无效 Authorization 不回落。Project 通过真实业务 scope 校验；伪造身份头/正文不授予权限。
- source_event_id 在同 Session 内幂等，Run ID 由 Host 生成；并发相同请求共享真实入场结果，不双执行。入场失败重复得到原失败，不伪排队。
- admission 只表示接收状态。没有 canonical command 时为 not_started；已有规范执行事实时，GET 和重复 POST 均以 Canonical events 为准，标记滞后不重放模型。
- 同 Session 跨显示 Surface 续聊；同 Project 多私聊独立；历史不限于旧 16 条截断。保存执行输入快照，公开消息/水位保留 Run 和输入/事件来源，内部上下文不直接暴露。
- 旧 ProductTask array 保持只读。新任务快照在 `{source}.tasks.v2.json`，Session/Run 关联在 `{source}.v2.json`；严格解析、原子写入、失败不污染已确认状态，修复坏目标后同 Host 可重试。
- 迁移以 workspace/actor/旧有效 Channel/来源类型/conversation 或 Run 确定性映射。conversation 与 run_id_fallback 同名不混；原 Run、conversation alias、source fallback/派生标记和时间保持；现有事件/工具/副作用不复制、不重放。
- dry-run 与 apply 共享目标校验/计数，重复均为零新增。公开摘要只基于最终获准集合，不包含其他 actor 或被拒绝 Project 的统计。旧源/目标冲突和坏源明确拒绝，源字节保留。

代码归属集中在 Host product-session/product-facade/workbench-session/workbench-migration、Python business scope/main 接线、公共契约/迁移回归，以及独立最小证据脚本/CI 接线；精确列表以本卡提交和冻结 source scope 为准。

## 审查与真实验证

两轴详情见 [WB-01-review](WB-01-review.md)。最终 admission 增量冻结 31 文件，patch SHA256 `88e6143a124eca9deffbd740b249adc64cae4162d2f04ff16e3ca719365e2e57`；两轴均核对 31/31 字节无漂移。最终验收文档另由主控更新，实施/测试字节必须匹配该快照。

- Standards：2 P2 → 0。水位比较原公开观察；Host 重建前正确关闭。
- Spec：3 P2 → 1 → 0。错误存在性、最终授权集合摘要、失败加载重试均经独立真实 HTTP 复核。
- 原试探测试、错误预期与先写后补测试的顺序已如实记录。它们不被包装成严格 test-first 或最终通过；修复依据为可核对的真实公共失败与正确目标回归。

| 检查 | 当前记录 |
| --- | --- |
| Host/Web build | 退出 0；Host 在最终源上重新构建 |
| 全仓 typecheck | `npm run typecheck` 退出 0；最终 admission 修复后再次通过 |
| Python 全套 | 1,080 passed，53 warnings，退出 0；相关 Python 源与依赖随后未变 |
| 首轮冻结 JS 全套 | 1,179 passed / 7 skipped，退出 0；仅对应修复前代码 |
| 第二轮 JS | 因剩余授权分支中止，退出 130；日志保留，不计通过 |
| 产品修复后 JS 候选 | 1,181 passed / 1 failed / 7 skipped，退出 1；迁移旧 5 秒观察窗失败，日志保留 |
| 迁移夹具修复后 JS | 1,181 passed / 1 failed / 7 skipped，退出 1；唯一 admission 旧 5 秒观察窗失败，迁移通过，日志保留 |
| 最终 admission 修复后 JS | 1,182 passed / 0 failed / 7 skipped，退出 0；`javascript-acceptance-admission-fixed.log` |
| release boundary / product smoke | 退出 0；product smoke 9 passed，为既有入口/启动源码测试，不冒充 UI live 验收 |
| WB-01 证据 CLI guard | 1 passed；排他目录拒绝覆盖 |
| 最终 AC-18b 新轮 | `wb01-acceptance-20260910-r1`：6 个 TS 文件 / 9 tests、Python scope 1 test，全部 pass/退出 0；sourceChanged=false、ownedChanged=false，manifest 独立核验 9 个文件通过 |

两次自然观察超时的具体慢层未确定。迁移与 admission 各自保留外部延迟 RED、有界观察 GREEN；只有 completed 可通过，产品预算未改。

原始本地运行日志保留在 `.tmp-tests/wb01/`；不夹带到公开提交。D/O 的合成数据、实际 Session/Run/source/event 水位和调用次数通过新轮的脱敏 runtime receipts 保存；usage unavailable，不补 0。真实模型与连接器未配置，L/M 保持 blocked；P 未运行。

## 最终增量与证据边界

[最终 increment result](../../../../evals/workbench/wb01/runs/wb01-acceptance-20260910-r1/evidence/increment-result.json) 与 [manifest](../../../../evals/workbench/wb01/runs/wb01-acceptance-20260910-r1/evidence/manifest.json) 固定本轮 source hashes、命令、失败标签和外部缺口。新轮 input dirty digest 为 `sha256:ec51a953f66c93a9f4e352491155d0e5d9b2f11f57006951a70a4c3b8fbd0c18`。主控已将证据中所有 22 个去重源码/依赖/脚本 hash 与上述代码提交的 Git blob 逐一比对，一致；38 个提交路径与明确 owned 清单一致。

脚本顶层只自动记 D，O 留给主控核定。主控根据真实 OMP worker、Canonical EventStore 与以下实际输出，将限定范围记为 D+O：

- `workbench-session-migration.json`：当前 actor 的 13 个旧 Run 映射为 5 个 Session；主 Session 含 9 个旧 Run，后续输入核对了早期用户限制与 assistant 历史，后续 Run 输入含原始 EUR 限制与 assistant 历史；共 14 次外部 fixture 调用。冷启动后逐 Run 水位一致，迁移/续聊/重复请求不改写旧来源。13 个原 Run 的 ID、来源与事件水位可追溯。
- `workbench-session-run.json`：跨 chat/create 的前两轮 Run/输入/水位，以及全测试 4 次 fixture 调用计数；后续重启和并发重复提交由测试断言验证。该精简 receipt 没有列出全部后续 Run ID，不称完整 Trace。
- `workbench-session-scope.json`：3 个 Session、2 个实际执行 Run/水位、2 次调用；同 Project 私聊与越权读取隔离。
- `workbench-admission-canonical.json`：实际 completed Run，标记滞后后重复提交仍只有 1 次模型调用。
- `workbench-session-contract.json` 与 `workbench-admission-failure.json`：D 范围的稳定身份/无模型入场失败，不冒充 OMP worker 执行。

全部 fixture 为合成业务数据，实际 Python 认证/业务持久层、Host/Runtime/EventStore 未 mock。模型 transport 是固定测试响应，usage unavailable；这些证据不评定真实模型质量、MCP 效果或 UI live 操作。两份历史诊断轮原字节保留。最终公开 20 个 JSON 均可解析，未发现本地绝对路径或凭据模式；release boundary 验证再次退出 0。

## 构建身份

主控在最终产品源码上重新执行 `npm run harness:v2:build`，退出 0，源码 pre/post hash 一致。Host 主 bundle SHA256 `05cc8bc0fbd838b5b9ac5e70329a362677d889a6cbcd322a98d5203500f91e57`（1,501,654 bytes），Web index SHA256 `19f90c39b7939be84c373862ca6b266b3211e1cea8aa8abe5e9064df34c3dcf6`。OMP manifest 的原文件 SHA256 为 `b6bd8ca3be24cd6e5db7756e9a0bbd162409654b907e54216d3288a0cf632833`，与其内部规范 `manifest.sha256` 是不同口径；运行包身份使用本文前述规范值。

本地 `build-acceptance-identity.json` 保留 build source 与产物 hash。验收 evidence 在 pre-change HEAD 加冻结 dirty 源上生成；后续代码提交应逐文件比对 `sourceHashesAfter` 与 `ownedHashesAfter`，不能仅凭 evidence 的旧 HEAD 字段推定对应提交。

## 基线、限制与下一张卡

基线仍为 WB-00 r5 / dataset `wb00-dataset-v1.1`，36 槽 21 blocked / 15 not_run；旧轮与所有历史诊断字节保留，不跨轮累加。WB-01 两个早期诊断轮包含缺 migration 的 not_run，及其中一轮的真实测试 RED，不用它们关账。

WB-01 已建立接口与稳定身份，不代表用户界面的普通输入已切到新 API。现有 Home/Create/Cowork/Crew 业务接口保留；其入口接入在 WB-03。`resource_refs` 非空当前明确 422，`requested_artifact` 保存三类请求元数据，实际能力/创建由后续卡接入。旧 Profile/Surface 工具上限、Skill 交集、问人/回答、等待终态处理仍是后续已知输入；不能据本票把它们标绿。

WB-02 首个可验收切片：通过当前 Workbench 公共提交链和真实 OMP，在同一已授权 Session 中发现/加载工具 A，观察不足后由模型选择工具 B；换 Surface 保持同类获准能力，撤权在实际调用前拒绝。按 R-01/04/05/15 与 AC-01/04/05/15 对应部分派 fresh Luna，主控负责职责与独立验收。

特别注意 v2 ProductTask 顶层 session_id/project_id/source 等新字段与旧 context 的消费者差异；能力/Gateway 应消费可信新绑定，不把任意 renderer 的 tool_catalog/workdir/system_prompt 当权限。需要资源引用时补真实业务解析；不要让新的目录只在独立测试面板可用。一个 Run 仍只有当前 Runtime 执行 authority，普通 Skill 文本不能扩权。旧 v1 policy 可有限兼容，v2 不继续以预选 Skill 作为通用能力上限。

不扩 Memory、Sandbox、WB-05～09，不移植 Codex SDK；继续本树 OMP。Provider/MCP 缺失继续可完成的 D/O，保留 L/M blocked。依协议每卡跨 fresh Supervisor 接力，不把余下全部工作留在同一 Session。

## 交接与回退

用户原有 AGENTS、CONTEXT、docs/agents 与规划内容（除已授权 STATUS）保持原字节；不整目录 stage、不 clean/reset。回退仅使用上一已验收构建和独立状态快照，保留新 v2 文件；不以旧 Python Agent 热回退。

全部 Coding、两轴 Reviewer、最终完整回归与证据进程已结束，目标测试/服务残留检查为空。本卡 18 个实现/测试/配置文件与 20 个公开证据 JSON 已提交，随后四份交接文档单独提交。主控创建下一任务后单次转交 STATUS owner。新主控在 STATUS 仍是本 owner 时只能只读等待；转交后本主控停止派工/修改。

交接前用户原有 tracked diff SHA256：`5a5a75dad632d126dad8073c8548b44508804e4329dc52d54696697da6c4611b`；AGENTS/CONTEXT 与 docs/agents/规划输入（除授权 STATUS）14 项 hash 保持，runtime lock 另核对一致。STATUS 是活跃所有权记录，最终 owner/HEAD 以其现场值为准；不将它夹带提交。
