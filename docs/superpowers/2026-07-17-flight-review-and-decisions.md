# Anna 飞行笔记 · 进度快照与决策清单

> 2026-07-17 · SIN → LAX 机上思考用 · Fable 5 汇编
> 用法建议:前 30 分钟读 A(事实)与 B(读法),然后逐条过 D(九个决策,每条有我的推荐,你只需要同意或推翻),精神好的时段写 C(两道只有你能写的创作题)。桌面有同内容的离线 HTML 副本(可勾选+记笔记):`Desktop\FLIGHT-NOTES-2026-07-17-anna.html`。

---

## A. 事实:现在在哪(2026-07-17 快照)

### A1 · 已在 main(f542a38,四门:pytest 709 / vitest 146 / tsc 0 / build ✓,6 gates)

按时间线,每一轮都已合并收口:

| 轮 | 交付 | 坐标 |
|---|---|---|
| R1 统一底座 | 5 个 LIVE surface 全在 forge 引擎 | b1e09b4 |
| R2 可观测 | 五面 Stage/Step + 三级下钻规格 | 随 Iris 合并入 main |
| 前端演进 | Lingee 吸收→夺舍→P3 优化→**Iris 重建**(主线前端) | 0e5a5e2 |
| Home 合并 | 一页双模/Composer 全套/运行屏/滑出面板,真流全验 | ed725da |
| **长跑轮 L1-L5** | 多轮会话/SQLite 持久化/后台 run+断线恢复/autocompact+续办/并发闸,验收记录 84b349d | **f542a38(07-17 合入)** |
| 治理文档 | 评测指导 v2(双轨/五支柱/pass^5 红线)· Crew PRD v1.0 拍板 · 判断力轮判定表(四轮学习唯一收敛出口) | 01-eval-guide / 87a42d5+8893e41 / 838a28b |

### A2 · 三条在飞的线(都活着,注意力要分配)

1. **判断力轮(feat/judgment-round,本仓当前分支)** — J1→J4 严格串行:
   - **J1 PlanGate**(计划守门):RED gate 已提交(44b5af2);GREEN 实现写了一半——`capability.py` +89 行**未提交**,另有未跟踪的 `tests/chat/test_plan_gate.py`。⚠️ 机前/机上若碰代码,先记得这两个文件是半成品。
   - **J2 Evaluator**(判断层核心,本轮价值主体):契约已写死——规则层前置(零成本路径)→ 独立上下文 LLM 法官 + 防伪造 runtime_facts → fail-open 永不阻塞终态 → 最多 1 次续跑 → 诚实 flagged。
   - **J3 Mid-run 插话**、**J4 出境披露 v1**(设置页「数据出境」+ 零遥测回归门)。
   - 判定表明确本轮=评测 P5 支柱的运行时落地;并已为 Crew 解除「长跑轮合 main」前置。
2. **Crew(feat/crew-build,Anna-crew worktree)** — **Claude Design 返稿 07-17 已回并归档**(3804a0e:设计稿 v1/v2 + 设计说明增补 + support.js;设计方把「2a 轻检视 / 2b 折叠导轨」标为二轮新增)。下一步 = 按 DESIGN-BRIEF §8 逐屏校对验收 → 出实施计划。
3. **问Anna 三面(feat/cowork-anna-polish)** — 已提交独立分支(6629a00:答复排版/运行动效/对话线程+memory recall),**未合 main,待你验收+真机走查**。

### A3 · 排队与等待

- **值守轮**(Cowork Finance×Hiker 重构):280 行 spec 完整,**卡在你的 6 项拍板**(§9,本文 D2 已逐项列出+建议)→ 拍板后才出 brief 交设计。
- **登录页**:brief 已交 Claude Design,**未返稿**。
- **R3 记忆轮**:蓝图已升级(FTS5 三层+衰减+Dream 蒸馏,Grok 解剖最工程化域);判定表裁定「差距最大域但独立成轮」。
- **R4 治理 + 0.3.0 发布 + 开源准备**:未开题。
- **Portfolio(个人项目,非 Anna)**:brief 已出,待拍板 语言/脱敏/站数。

### A4 · 债务登记(不决策,只记账)

answer-and-ask 验收走查 · Home 轮偏差 6 条(00-plan.md)· Iris 三缺口(真字体打包/LoopCard 真跑/容器统一)· demo-erp 生产 HTTPS · graphify 图 06-30 停更待 --update · W9 Windows 沙箱(判定表:**不提前**,xAI 也是 no-op,有佐证,维持里程碑)。

---

## B. 读法:主线叙事与当前的结构性张力

**主线叙事(产品专家视角):Anna 的每一轮都在拉长「可信任务地平线」**——这与你评测文档的北极星(task horizon / pass^5)完全咬合:

> R1 给了统一体格 → Iris/Home 给了身体和门面 → 长跑轮让它**跑得久**(L1-L5)→ 判断力轮让它**跑得对**(Evaluator=完成质量)→ 记忆轮让它**记得住**(跨 run 复利)→ Crew 让**一群它协同**(组织地平线)。

**当前的结构性张力(这次飞行最该想的一件事)**:单 Agent 纵深(判断/记忆/评测)与组织横向(Crew)**第一次出现资源竞争**。以前每轮只有一个"显然的下一步",现在有三个都成立的候选,而且 Crew 设计稿刚回、是热的。D1 就是这道题。

第二个张力:**你自己的评测方法论(Phase 0 先立标尺)与实际节奏(轮子一个接一个、Phase 0 一直没排上)已经背离了两轮**。J2 Evaluator 的 audit 链(`run.evaluation.*`)恰好就是 P5 的数据源——判断力轮收尾是插入 Phase 0 成本最低的时机,再往后拖,Crew 这种最大的轮就要在没有标尺的底座上开工。

第三个观察:**「注意力」正在成为 Anna 的第二个产品中心**。值守轮(业务找你)和 Crew(通知铃/auto-wake)都在建"需要你的事来找你"的面。通知底座只该建一套(判定表已把 auto-wake 指到 Crew 站内铃)。D3 就是这道题。

---

## C. 两道创作题(只有你能写,飞机是最好的场合)

### C1 · Anna 的 SOUL / IDENTITY / USER 人格文件初稿 ★强烈建议

判断力轮判定表原文:「**人格内容是用户的拍板件,不该由我代拟**;J2 v1 判据先用客观完成纪律,SOUL 落地后接为判据」。也就是说:你写的这份文件,未来会直接变成 Evaluator 的判断价值锚(冻结价值锚,防自我进化漂移)。提纲建议:

- **Anna 是谁**:一段话身份(企业级 Harness AIOS?你的同事?);对「拟人皮不盖真状态」的第一人称承诺。
- **永不做什么**:诚实红线的第一人称化(不编数字、不假成功、不站位、数据不出你配置的边界——J4 的披露文案可以直接引用这里)。
- **判断优先级排序**:诚实 > 完成 > 速度?遇到"完不成"时的姿态(诚实标注 vs 硬凑)。
- **对用户的姿态**:主动到什么程度(值守/插话/催收都涉及"打扰权")。
- **USER 侧**:Anna 应该永远记得你的什么(语言/术语/拍板习惯/红线)。

### C2 · 开源 README 第一屏草稿

一句话定位 + 首五分钟体验 + 「无遥测、无训练回传、记忆全本地」承诺(J4 出境披露正是这个叙事的产品化)。写的时候会自然逼出 D4/D5 的答案。

---

## D. 决策清单(九项,按重要度排序;每项:背景→选项→我的推荐)

### D1 ★★★ 判断力轮之后,下一轮是什么?

三个都成立的候选:

| 选项 | 论据 | 风险 |
|---|---|---|
| a) Crew 实施先行 | 设计返稿是热的;PRD 刚拍板动能足;旗舰场景(功能迭代与设计 SOP)是给 Andy/外界看的最强叙事 | 三面一体里的 Memory 面没有底座,可能被迫站位(违背零站位红线);最大的轮开在没有评测标尺的底座上 |
| b) R3 记忆轮先行 | 复利最大(所有 surface 受益);蓝图现成(FTS5+Dream);J2 的 verdict 是天然的记忆写入时机(判断→记忆有一条隐藏接线) | Crew 设计稿凉置,校对反馈周期拉长;连续第三个"底座轮",可见的新东西少 |
| c) 评测 Phase 0 先行(~1 周) | 你自己的方法论要求(先立标尺再大开发);J2 audit 链正好是 P5 数据源;时机唯一——Crew 之前 | 纯基建,无用户可见功能 |

**我的推荐(串法,不是三选一)**:判断力轮收尾 → **Phase 0 立标尺(~1 周)+ Crew 返稿 §8 校对并行**(校对是文档活,不抢开发位)→ **Crew 实施**(其 Memory 面按 R3 蓝图做"最小真实底座",一套底座两面用,Crew 需要多少建多少,不站位也不重复建设)→ 记忆轮全量(补 Dream/衰减/三层完整形态)。
**机上要定的**:同意这个串法吗?如果不同意,a 和 b 谁先?Memory 面在 Crew 里做"最小真实"还是等全量记忆轮?

### D2 ★★ 值守轮 6 项拍板(spec §9,可直接机上勾掉)

| # | 决策点 | 我的建议 |
|---|---|---|
| 1 | 值守页作为 Cowork 默认落点? | **是**(整个取向 B 的立意就是"打开就有值守结果";保守方案会削弱叙事) |
| 2 | 派办催收闭环进 P1? | **进**(旗舰差异化;审批门/写工具对/ApprovalCard 零件全现成;连带 Associate 干净退役) |
| 3 | 客户/合同 360 形态 | **右侧抽屉**(与副驾同容器规格 420px;独立页太重) |
| 4 | 报销就地审批节奏 | **P2 确认**(P1 深链报销页够用;W4 通用 ApprovalCard 成熟后再就地) |
| 5 | Cowork 段标签 | **「看板」→「值守」**(重构后它确实不止是看板;「业务」太泛) |
| 6 | 值守页要不要自己的副驾? | **P1 不要**(追问路由到对应系统副驾;避免第四个 ReAct 面;P2 视用感再议) |

注意:拍板后值守轮也只是"具备出 brief 条件",什么时候真排进队列受 D1 影响——按我的串法它在 Crew 之后,或与 Crew 错峰走设计线(设计返稿周期长,早交 brief 不亏)。**建议:机上拍完 6 项,回来就出 brief 交设计,实施档期另说**——设计线和开发线是两条队列。

### D3 ★★ 值守 × Crew:一个注意力中心还是两个面?

值守=业务经 Anna 找你(个人业务收件箱);Crew=组织工作图找你(站内通知铃/auto-wake)。两者都在建"需要你的事送上门"。
**我的建议**:**一套通知底座,两个投影面**——底层事件/订阅/铃铛机制只建一次(Crew 轮建,因为判定表已把 auto-wake 指到那里),值守 P2 的定时巡检结果作为一种通知源接入。机上想清楚词表:「值守卡」「通知」「待办」三个词的边界,避免两轮各造一套名词。

### D4 ★★ 开源 0.3.0 的 bar 是什么?

要定的子项:
- **0.3.0 = 开源版吗**,还是先私仓打磨到 0.4?
- **必须真清单**:LICENSE 选型 / secrets 全仓扫描 / README 故事 / **首五分钟体验**——陌生人 clone 后没有 demo-erp、没有任何 MCP token、没有模型 key 时,看到的是优雅的诚实空态还是一堆报错?(这其实是对"诚实三态"体系的终极检验)
- **Windows-first 与 OSS 受众(mac/linux 为主)的矛盾**:0.3.0 解决还是 README 里诚实声明 "Windows-first, mac/linux community-tested"?
- demo 数据方案:demo-erp 随仓附带(它已是独立 deploy-ready 仓)还是 mock 模式?
**我的倾向**:0.3.0 定为"可开源质量"但**先不公开**,留一个 0.3.x 窗口给 secrets 扫描+README+首五分钟打磨;Windows-first 诚实声明而非强行跨平台。

### D5 ★ 一句话定位 + 旗舰 demo 选谁?

三个候选旗舰:Home 长跑构建(个人 Harness)/ 值守派办催收(业务 AIOS)/ Crew 功能迭代 SOP(组织协同)。
**牵连**:开源 README、给 Andy 的演示、你的求职叙事(portfolio 终点站=「个人 Harness Agent 上云可上手」)最好共用一个主叙事。
**我的倾向**:主叙事=**「模型无关的企业级 Agent Runtime(Harness AIOS):从一次对话到一个组织」**,旗舰 demo 按受众切换——技术受众看长跑+判断(可观测三级下钻是杀手镜头),业务受众看值守派办,愿景讲 Crew。机上把"一句话"写出来(C2 会逼你写)。
**顺带一致性检查**:「模型无关」承诺目前 model_profiles 仅 chat 面兑现——开源前要么补齐要么措辞降级,别让 README 第一句就站位。

### D6 ★ 记忆 v1 范围(为 D1 的串法服务)

蓝图有:三层记忆 / FTS5 检索 / 衰减 / Dream 后台蒸馏。要定:
- v1 砍到哪:我的建议——**写入+FTS5 检索先行,衰减与 Dream 后置**(Dream 依赖后台调度,和调度器 D8 合并考虑)。
- **写入者优先级**:J2 evaluator 的 verdict(自动)/ 用户显式「记住」(显式)/ Dream 蒸馏(后台)——建议 v1 只做前两个。
- 边界:per-user / per-agent / per-surface 怎么分层;Crew 的 org memory 与个人 memory 是同一套底座的两个 scope 还是两套?(建议:同底座不同 scope。)
- 注入纪律:继承判断力轮的 KV-cache 红线——**记忆注入一律走消息,不动系统提示**。

### D7 ★ 异步化时机

架构对齐记忆:「今天代码部分串行待异步化」;L5 并发闸是闸、不是异步化。Crew 3 个 Agent 账号 + 调度器会逼出真异步。
**我的建议**:**随 Crew 轮做,不独立成轮**——有真需求驱动才不过度设计。机上只需要认可这个原则。

### D8 ★ 调度器形态(值守 P2 与 Crew 共同依赖)

全仓当前零调度零件。值守要定时巡检/哨兵,Crew 要 SOP 定时节点,Dream 要后台窗口。
**我的建议**:一套调度底座(进程内定时器 + SQLite job 表起步,不引外部依赖),Crew 轮顺带建,值守 P2 与 Dream 复用。机上想:job 的可观测性要不要也走 run/journal 语法(我倾向要——"巡检也是一个 run"值守 spec 已经这么写了)。

### D9 ○ Portfolio 三项拍板(个人线,顺手定)

语言(中/英/双)/ 脱敏程度 / 站数(单页 vs 多页)。与 LA 之行的求职语境正相关,机上顺手定掉,回来我就能推进。

---

## E. 回来后第一天(建议动作序)

1. 把机上的 D1-D9 决定回灌给我(哪怕是照片/几行字),我回写各 spec 与记忆。
2. **验收积压**:answer-and-ask 真机走查(feat/cowork-anna-polish,6629a00)→ 合 main;判断力轮 J1 续写。
3. 值守轮 6 项已拍 → 我出 DESIGN-BRIEF 交 Claude Design(设计线错峰跑起来)。
4. Crew 返稿 §8 校对开工(可与判断力轮并行,文档活)。
5. C1 的 SOUL 初稿给我 → 我落成人格文件的工程形态(记忆/人格轮的第一块砖)。

## F. 附:证据与坐标

- main = f542a38(长跑轮合入;四门 pytest 709 / vitest 146 / tsc 0 / build ✓;gates 6)
- 分支:feat/judgment-round(44b5af2 + 未提交 capability.py +89/tests/chat/test_plan_gate.py)· feat/crew-build @ Anna-crew worktree(3804a0e 设计返稿归档)· feat/cowork-anna-polish(6629a00+116b82b,未合)
- 判断力轮判定表+切片契约:`docs/superpowers/plans/2026-07-17-judgment-round/00-plan.md`
- 值守轮 spec(§9 拍板清单):`docs/superpowers/specs/2026-07-12-cowork-duty-station-design.md`
- 评测指导 v2(Phase 0 定义):`docs/superpowers/plans/2026-07-12-harness-longrun/01-eval-and-optimization-guide.md`
- Crew 返稿:`docs/design/2026-07-17-crew-return/`(feat/crew-build 分支)
- Crew PRD 拍板:87a42d5 + 8893e41;长跑轮验收记录:84b349d
